generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "mysql"
}

model Role {
  id           String   @id @default(uuid()) @db.Char(36)
  code         String   @unique @db.VarChar(50)
  name         String   @db.VarChar(100)
  description  String?  @db.VarChar(500)
  isSystemRole Boolean  @default(false) @map("is_system_role")
  status       String   @default("ACTIVE") @db.VarChar(30)
  createdAt    DateTime @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt    DateTime @updatedAt @map("updated_at") @db.DateTime(3)

  userRoles UserRole[]

  @@map("roles")
}

model User {
  id                  String    @id @default(uuid()) @db.Char(36)
  email               String    @unique @db.VarChar(255)
  passwordHash        String    @map("password_hash") @db.VarChar(255)
  firstName           String    @map("first_name") @db.VarChar(100)
  lastName            String?   @map("last_name") @db.VarChar(100)
  phoneCountryCode    String?   @map("phone_country_code") @db.VarChar(10)
  phoneNumber         String?   @map("phone_number") @db.VarChar(30)
  avatarMediaId       String?   @map("avatar_media_id") @db.Char(36)
  status              String    @default("ACTIVE") @db.VarChar(30)
  emailVerifiedAt     DateTime? @map("email_verified_at") @db.DateTime(3)
  passwordChangedAt   DateTime? @map("password_changed_at") @db.DateTime(3)
  failedLoginAttempts Int       @default(0) @map("failed_login_attempts")
  lockedUntil         DateTime? @map("locked_until") @db.DateTime(3)
  lastLoginAt         DateTime? @map("last_login_at") @db.DateTime(3)
  lastLoginIp         String?   @map("last_login_ip") @db.VarChar(45)
  timezone            String    @default("Asia/Kolkata") @db.VarChar(100)
  locale              String    @default("en-IN") @db.VarChar(20)
  createdAt           DateTime  @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt           DateTime  @updatedAt @map("updated_at") @db.DateTime(3)
  deletedAt           DateTime? @map("deleted_at") @db.DateTime(3)

  avatarMedia                  MediaAsset?                    @relation("UserAvatar", fields: [avatarMediaId], references: [id], onDelete: SetNull)
  userRoles                    UserRole[]
  assignedUserRoles            UserRole[]                     @relation("UserRoleAssigner")
  refreshTokens                RefreshToken[]
  passwordResetTokens          PasswordResetToken[]
  emailVerificationTokens      EmailVerificationToken[]
  studentProfile               StudentProfile?
  studentMessages              StudentMessage[]               @relation("StudentMessageSender")
  internalMessages             StudentMessage[]               @relation("InternalMessageSender")
  applicationStatusChanges     StudentApplicationTimeline[]   @relation("ApplicationStatusChangedBy")
  scholarshipStatusChanges     StudentScholarshipTimeline[]   @relation("ScholarshipStatusChangedBy")
  supportStatusChanges         StudentSupportTicket[]         @relation("SupportStatusChangedBy")
  supportMessages              StudentSupportMessage[]        @relation("SupportMessageSender")
  loginAttempts                LoginAttempt[]
  auditLogs                    AuditLog[]
  uploadedMedia                MediaAsset[]                   @relation("MediaUploadedBy")
  updatedSettings              SiteSetting[]                  @relation("SiteSettingUpdatedBy")
  createdPages                 Page[]                         @relation("PageCreatedBy")
  updatedPages                 Page[]                         @relation("PageUpdatedBy")
  createdPageTemplates         PageTemplate[]                 @relation("PageTemplateCreatedBy")
  updatedPageTemplates         PageTemplate[]                 @relation("PageTemplateUpdatedBy")
  contentVersions              ContentVersion[]               @relation("ContentVersionCreatedBy")
  createdExperiments           Experiment[]                   @relation("ExperimentCreatedBy")
  createdSubjects              Subject[]                      @relation("SubjectCreatedBy")
  updatedSubjects              Subject[]                      @relation("SubjectUpdatedBy")
  createdSubSubjects           SubSubject[]                   @relation("SubSubjectCreatedBy")
  updatedSubSubjects           SubSubject[]                   @relation("SubSubjectUpdatedBy")
  createdCountries             Country[]                      @relation("CountryCreatedBy")
  updatedCountries             Country[]                      @relation("CountryUpdatedBy")
  createdCities                City[]                         @relation("CityCreatedBy")
  updatedCities                City[]                         @relation("CityUpdatedBy")
  createdCountryFaqs           CountryFaq[]                   @relation("CountryFaqCreatedBy")
  updatedCountryFaqs           CountryFaq[]                   @relation("CountryFaqUpdatedBy")
  createdCourses               Course[]                       @relation("CourseCreatedBy")
  updatedCourses               Course[]                       @relation("CourseUpdatedBy")
  assignedLeads                Lead[]                         @relation("LeadAssignedTo")
  assignedBookings             CounsellingBooking[]           @relation("BookingAssignedTo")
  reviewedUniversityClaims     UniversityClaimRequest[]       @relation("UniversityClaimReviewedBy")
  universityClaimNotes         UniversityClaimNote[]
  universityClaimStatusChanges UniversityClaimStatusHistory[] @relation("UniversityClaimStatusChangedBy")
  leadNotes                    LeadNote[]
  changedLeadStatuses          LeadStatusHistory[]            @relation("LeadStatusChangedBy")
  createdEmailTemplates        EmailTemplate[]                @relation("EmailTemplateCreatedBy")
  updatedEmailTemplates        EmailTemplate[]                @relation("EmailTemplateUpdatedBy")
  createdRedirects             Redirect[]                     @relation("RedirectCreatedBy")
  createdContinents            Continent[]                    @relation("ContinentCreatedBy")
  updatedContinents            Continent[]                    @relation("ContinentUpdatedBy")
  convertedContactInquiries    ContactInquiry[]               @relation("ContactInquiryConvertedBy")

  @@index([avatarMediaId])
  @@index([status, deletedAt])
  @@map("users")
}

model UserRole {
  id               String   @id @default(uuid()) @db.Char(36)
  userId           String   @map("user_id") @db.Char(36)
  roleId           String   @map("role_id") @db.Char(36)
  assignedByUserId String?  @map("assigned_by_user_id") @db.Char(36)
  assignedAt       DateTime @default(now()) @map("assigned_at") @db.DateTime(3)

  user       User  @relation(fields: [userId], references: [id], onDelete: Cascade)
  role       Role  @relation(fields: [roleId], references: [id], onDelete: Restrict)
  assignedBy User? @relation("UserRoleAssigner", fields: [assignedByUserId], references: [id], onDelete: SetNull)

  @@unique([userId, roleId])
  @@index([userId])
  @@index([roleId])
  @@index([assignedByUserId])
  @@map("user_roles")
}

model RefreshToken {
  id                String    @id @default(uuid()) @db.Char(36)
  userId            String    @map("user_id") @db.Char(36)
  tokenHash         String    @unique @map("token_hash") @db.VarChar(255)
  expiresAt         DateTime  @map("expires_at") @db.DateTime(3)
  revokedAt         DateTime? @map("revoked_at") @db.DateTime(3)
  revocationReason  String?   @map("revocation_reason") @db.VarChar(255)
  replacedByTokenId String?   @map("replaced_by_token_id") @db.Char(36)
  /// How many read-only session validations this token has satisfied *after*
  /// being rotated away. Bounded so a superseded token cannot be replayed
  /// indefinitely by a navigation that raced the rotation.
  graceUseCount     Int       @default(0) @map("grace_use_count")
  createdIp         String?   @map("created_ip") @db.VarChar(45)
  userAgent         String?   @map("user_agent") @db.VarChar(1000)
  createdAt         DateTime  @default(now()) @map("created_at") @db.DateTime(3)

  user            User           @relation(fields: [userId], references: [id], onDelete: Cascade)
  replacedByToken RefreshToken?  @relation("RefreshTokenReplacement", fields: [replacedByTokenId], references: [id], onDelete: SetNull)
  replaces        RefreshToken[] @relation("RefreshTokenReplacement")

  @@index([userId])
  @@index([replacedByTokenId])
  @@map("refresh_tokens")
}

model PasswordResetToken {
  id          String    @id @default(uuid()) @db.Char(36)
  userId      String    @map("user_id") @db.Char(36)
  tokenHash   String    @unique @map("token_hash") @db.VarChar(255)
  expiresAt   DateTime  @map("expires_at") @db.DateTime(3)
  usedAt      DateTime? @map("used_at") @db.DateTime(3)
  requestedIp String?   @map("requested_ip") @db.VarChar(45)
  createdAt   DateTime  @default(now()) @map("created_at") @db.DateTime(3)

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
  @@map("password_reset_tokens")
}

/// Proves a person controls the address they registered with. Only the hash is
/// stored, so a leaked row cannot be replayed; `usedAt` makes a token
/// single-use and `expiresAt` bounds the window.
model EmailVerificationToken {
  id          String    @id @default(uuid()) @db.Char(36)
  userId      String    @map("user_id") @db.Char(36)
  tokenHash   String    @unique @map("token_hash") @db.VarChar(255)
  email       String    @db.VarChar(255)
  expiresAt   DateTime  @map("expires_at") @db.DateTime(3)
  usedAt      DateTime? @map("used_at") @db.DateTime(3)
  requestedIp String?   @map("requested_ip") @db.VarChar(45)
  createdAt   DateTime  @default(now()) @map("created_at") @db.DateTime(3)

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
  @@index([userId, createdAt])
  @@map("email_verification_tokens")
}

model LoginAttempt {
  id             String   @id @default(uuid()) @db.Char(36)
  userId         String?  @map("user_id") @db.Char(36)
  attemptedEmail String   @map("attempted_email") @db.VarChar(255)
  wasSuccessful  Boolean  @map("was_successful")
  failureReason  String?  @map("failure_reason") @db.VarChar(255)
  ipAddress      String?  @map("ip_address") @db.VarChar(45)
  userAgent      String?  @map("user_agent") @db.VarChar(1000)
  requestId      String?  @map("request_id") @db.VarChar(100)
  createdAt      DateTime @default(now()) @map("created_at") @db.DateTime(3)

  user User? @relation(fields: [userId], references: [id], onDelete: SetNull)

  @@index([userId])
  @@index([attemptedEmail, createdAt])
  @@index([ipAddress, createdAt])
  @@map("login_attempts")
}

model AuditLog {
  id          String   @id @default(uuid()) @db.Char(36)
  userId      String?  @map("user_id") @db.Char(36)
  module      String   @db.VarChar(100)
  entityType  String   @map("entity_type") @db.VarChar(100)
  entityId    String?  @map("entity_id") @db.Char(36)
  action      String   @db.VarChar(50)
  oldValues   Json?    @map("old_values") @db.Json
  newValues   Json?    @map("new_values") @db.Json
  description String?  @db.VarChar(1000)
  ipAddress   String?  @map("ip_address") @db.VarChar(45)
  userAgent   String?  @map("user_agent") @db.VarChar(1000)
  requestId   String?  @map("request_id") @db.VarChar(100)
  createdAt   DateTime @default(now()) @map("created_at") @db.DateTime(3)

  user User? @relation(fields: [userId], references: [id], onDelete: SetNull)

  @@index([userId, createdAt])
  @@index([entityType, entityId])
  @@index([module, action, createdAt])
  @@map("audit_logs")
}

model MediaAsset {
  id               String    @id @default(uuid()) @db.Char(36)
  storageProvider  String    @default("LOCAL") @map("storage_provider") @db.VarChar(30)
  bucketName       String?   @map("bucket_name") @db.VarChar(255)
  objectKey        String    @map("object_key") @db.VarChar(1000)
  publicUrl        String    @map("public_url") @db.VarChar(2048)
  originalFileName String    @map("original_file_name") @db.VarChar(500)
  storedFileName   String    @map("stored_file_name") @db.VarChar(500)
  mimeType         String    @map("mime_type") @db.VarChar(150)
  fileExtension    String?   @map("file_extension") @db.VarChar(20)
  fileSizeBytes    BigInt    @map("file_size_bytes") @db.BigInt
  width            Int?      @db.Int
  height           Int?      @db.Int
  durationSeconds  Int?      @map("duration_seconds") @db.Int
  checksum         String?   @db.VarChar(128)
  title            String?   @db.VarChar(255)
  altText          String?   @map("alt_text") @db.VarChar(500)
  caption          String?   @db.Text
  folder           String?   @db.VarChar(255)
  mediaType        String    @default("IMAGE") @map("media_type") @db.VarChar(30)
  status           String    @default("ACTIVE") @db.VarChar(30)
  uploadedByUserId String?   @map("uploaded_by_user_id") @db.Char(36)
  createdAt        DateTime  @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt        DateTime  @updatedAt @map("updated_at") @db.DateTime(3)
  deletedAt        DateTime? @map("deleted_at") @db.DateTime(3)

  uploadedBy               User?                   @relation("MediaUploadedBy", fields: [uploadedByUserId], references: [id], onDelete: SetNull)
  avatarUsers              User[]                  @relation("UserAvatar")
  pageSections             PageSection[]           @relation("PageSectionMedia")
  backgroundSections       PageSection[]           @relation("PageSectionBackgroundMedia")
  navigationIcons          NavigationItem[]        @relation("NavigationItemIconMedia")
  platformMetrics          PlatformMetric[]        @relation("PlatformMetricIconMedia")
  seoOgMetadata            SeoMetadata[]           @relation("SeoOgMedia")
  seoTwitterMetadata       SeoMetadata[]           @relation("SeoTwitterMedia")
  continentIcons           Continent[]             @relation("ContinentIconMedia")
  continentHeroes          Continent[]             @relation("ContinentHeroMedia")
  subjectIcons             Subject[]               @relation("SubjectIconMedia")
  subjectListingMedia      Subject[]               @relation("SubjectListingMedia")
  subjectHeroMedia         Subject[]               @relation("SubjectHeroMedia")
  subSubjectIcons          SubSubject[]            @relation("SubSubjectIconMedia")
  subSubjectListingMedia   SubSubject[]            @relation("SubSubjectListingMedia")
  countryFlags             Country[]               @relation("CountryFlagMedia")
  countryListingMedia      Country[]               @relation("CountryListingMedia")
  countryHeroMedia         Country[]               @relation("CountryHeroMedia")
  countryMapMedia          Country[]               @relation("CountryMapMedia")
  cityHeroMedia            City[]                  @relation("CityHeroMedia")
  countryPrimarySections   CountryContentSection[] @relation("CountryPrimaryMedia")
  countrySecondarySections CountryContentSection[] @relation("CountrySecondaryMedia")
  courseFeaturedMedia      Course[]                @relation("CourseFeaturedMedia")
  courseSections           CourseContentSection[]  @relation("CourseSectionMedia")
  consultantIcons          ConsultantLandingCard[] @relation("ConsultantIconMedia")
  consultantFeaturedMedia  ConsultantLandingCard[] @relation("ConsultantFeaturedMedia")

  studentEnglishTestResults StudentEnglishTest[] @relation("StudentEnglishTestResult")
  studentPassportScans      StudentPassport[]    @relation("StudentPassportScan")
  studentDocuments          StudentDocument[]    @relation("StudentDocumentMedia")
  applicationOffers         StudentApplication[] @relation("StudentApplicationOfferMedia")

  @@index([uploadedByUserId])
  @@index([folder, status])
  @@index([checksum])
  @@map("media_assets")
}

model SiteSetting {
  id              String   @id @default(uuid()) @db.Char(36)
  settingKey      String   @unique @map("setting_key") @db.VarChar(150)
  settingGroup    String   @map("setting_group") @db.VarChar(100)
  valueType       String   @map("value_type") @db.VarChar(30)
  valueJson       Json?    @map("value_json") @db.Json
  description     String?  @db.VarChar(500)
  isPublic        Boolean  @default(false) @map("is_public")
  updatedByUserId String?  @map("updated_by_user_id") @db.Char(36)
  createdAt       DateTime @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt       DateTime @updatedAt @map("updated_at") @db.DateTime(3)

  updatedBy User? @relation("SiteSettingUpdatedBy", fields: [updatedByUserId], references: [id], onDelete: SetNull)

  @@index([updatedByUserId])
  @@index([settingGroup, isPublic])
  @@map("site_settings")
}

model FeatureFlag {
  id                String   @id @default(uuid()) @db.Char(36)
  flagKey           String   @unique @map("flag_key") @db.VarChar(150)
  name              String   @db.VarChar(255)
  description       String?  @db.VarChar(500)
  isEnabled         Boolean  @default(false) @map("is_enabled")
  environment       String   @default("ALL") @db.VarChar(30)
  configurationJson Json?    @map("configuration_json") @db.Json
  createdAt         DateTime @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt         DateTime @updatedAt @map("updated_at") @db.DateTime(3)

  @@map("feature_flags")
}

model Page {
  id               String    @id @default(uuid()) @db.Char(36)
  pageType         String    @map("page_type") @db.VarChar(50)
  title            String    @db.VarChar(255)
  shortTitle       String?   @map("short_title") @db.VarChar(150)
  slug             String    @unique @db.VarChar(255)
  layoutKey        String?   @map("layout_key") @db.VarChar(100)
  templateId       String?   @map("template_id") @db.Char(36)
  shortDescription String?   @map("short_description") @db.VarChar(1000)
  /// Optional per-page Header/Footer override. NULL means "use the Global
  /// Header/Footer", which is what every existing page resolves to, so adding
  /// this column changes nothing that is already published.
  chromeConfigJson Json?     @map("chrome_config_json") @db.Json
  status           String    @default("DRAFT") @db.VarChar(30)
  isHomepage       Boolean   @default(false) @map("is_homepage")
  displayOrder     Int       @default(0) @map("display_order")
  publishedAt      DateTime? @map("published_at") @db.DateTime(3)
  startsAt         DateTime? @map("starts_at") @db.DateTime(3)
  endsAt           DateTime? @map("ends_at") @db.DateTime(3)
  createdByUserId  String?   @map("created_by_user_id") @db.Char(36)
  updatedByUserId  String?   @map("updated_by_user_id") @db.Char(36)
  createdAt        DateTime  @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt        DateTime  @updatedAt @map("updated_at") @db.DateTime(3)
  deletedAt        DateTime? @map("deleted_at") @db.DateTime(3)

  createdBy       User?            @relation("PageCreatedBy", fields: [createdByUserId], references: [id], onDelete: SetNull)
  updatedBy       User?            @relation("PageUpdatedBy", fields: [updatedByUserId], references: [id], onDelete: SetNull)
  template        PageTemplate?    @relation(fields: [templateId], references: [id], onDelete: SetNull)
  sections        PageSection[]
  navigationItems NavigationItem[]

  @@index([createdByUserId])
  @@index([updatedByUserId])
  @@index([pageType, status, displayOrder])
  @@index([templateId])
  @@map("pages")
}

model PageTemplate {
  id                  String    @id @default(uuid()) @db.Char(36)
  name                String    @db.VarChar(255)
  templateKey         String    @unique @map("template_key") @db.VarChar(100)
  description         String?   @db.VarChar(1000)
  pageFamily          String    @map("page_family") @db.VarChar(60)
  defaultSectionsJson Json      @map("default_sections_json") @db.Json
  layoutConfigJson    Json?     @map("layout_config_json") @db.Json
  /// Same contract as Page.chromeConfigJson, applied to every dynamic detail
  /// page rendered through this template. A page's own override wins over it.
  chromeConfigJson    Json?     @map("chrome_config_json") @db.Json
  isActive            Boolean   @default(true) @map("is_active")
  createdByUserId     String?   @map("created_by_user_id") @db.Char(36)
  updatedByUserId     String?   @map("updated_by_user_id") @db.Char(36)
  createdAt           DateTime  @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt           DateTime  @updatedAt @map("updated_at") @db.DateTime(3)
  deletedAt           DateTime? @map("deleted_at") @db.DateTime(3)

  createdBy User?  @relation("PageTemplateCreatedBy", fields: [createdByUserId], references: [id], onDelete: SetNull)
  updatedBy User?  @relation("PageTemplateUpdatedBy", fields: [updatedByUserId], references: [id], onDelete: SetNull)
  pages     Page[]

  @@index([pageFamily, isActive])
  @@index([createdByUserId])
  @@index([updatedByUserId])
  @@map("page_templates")
}

model PageSection {
  id                String    @id @default(uuid()) @db.Char(36)
  pageId            String    @map("page_id") @db.Char(36)
  sectionKey        String    @map("section_key") @db.VarChar(100)
  sectionType       String    @map("section_type") @db.VarChar(100)
  eyebrow           String?   @db.VarChar(255)
  heading           String?   @db.VarChar(500)
  subheading        String?   @db.Text
  bodyJson          Json?     @map("body_json") @db.Json
  mediaId           String?   @map("media_id") @db.Char(36)
  backgroundMediaId String?   @map("background_media_id") @db.Char(36)
  ctaPrimaryLabel   String?   @map("cta_primary_label") @db.VarChar(100)
  ctaPrimaryUrl     String?   @map("cta_primary_url") @db.VarChar(1000)
  ctaSecondaryLabel String?   @map("cta_secondary_label") @db.VarChar(100)
  ctaSecondaryUrl   String?   @map("cta_secondary_url") @db.VarChar(1000)
  configurationJson Json?     @map("configuration_json") @db.Json
  displayOrder      Int       @default(0) @map("display_order")
  status            String    @default("ACTIVE") @db.VarChar(30)
  startsAt          DateTime? @map("starts_at") @db.DateTime(3)
  endsAt            DateTime? @map("ends_at") @db.DateTime(3)
  createdAt         DateTime  @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt         DateTime  @updatedAt @map("updated_at") @db.DateTime(3)
  deletedAt         DateTime? @map("deleted_at") @db.DateTime(3)

  page            Page         @relation(fields: [pageId], references: [id], onDelete: Cascade)
  media           MediaAsset?  @relation("PageSectionMedia", fields: [mediaId], references: [id], onDelete: SetNull)
  backgroundMedia MediaAsset?  @relation("PageSectionBackgroundMedia", fields: [backgroundMediaId], references: [id], onDelete: SetNull)
  experiments     Experiment[]

  @@unique([pageId, sectionKey])
  @@index([pageId, status, displayOrder])
  @@index([mediaId])
  @@index([backgroundMediaId])
  @@map("page_sections")
}

model Experiment {
  id              String    @id @default(uuid()) @db.Char(36)
  key             String    @unique @db.VarChar(150)
  name            String    @db.VarChar(255)
  description     String?   @db.VarChar(1000)
  sectionId       String    @map("section_id") @db.Char(36)
  status          String    @default("DRAFT") @db.VarChar(30)
  startsAt        DateTime? @map("starts_at") @db.DateTime(3)
  endsAt          DateTime? @map("ends_at") @db.DateTime(3)
  createdByUserId String?   @map("created_by_user_id") @db.Char(36)
  createdAt       DateTime  @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt       DateTime  @updatedAt @map("updated_at") @db.DateTime(3)
  deletedAt       DateTime? @map("deleted_at") @db.DateTime(3)

  section     PageSection            @relation(fields: [sectionId], references: [id], onDelete: Cascade)
  createdBy   User?                  @relation("ExperimentCreatedBy", fields: [createdByUserId], references: [id], onDelete: SetNull)
  variants    ExperimentVariant[]
  exposures   ExperimentExposure[]
  conversions ExperimentConversion[]

  @@index([sectionId, status])
  @@index([createdByUserId])
  @@map("experiments")
}

model ExperimentVariant {
  id              String    @id @default(uuid()) @db.Char(36)
  experimentId    String    @map("experiment_id") @db.Char(36)
  key             String    @db.VarChar(100)
  name            String    @db.VarChar(255)
  isControl       Boolean   @default(false) @map("is_control")
  trafficWeight   Int       @default(0) @map("traffic_weight")
  eyebrow         String?   @db.VarChar(255)
  heading         String?   @db.VarChar(500)
  subheading      String?   @db.Text
  ctaPrimaryLabel String?   @map("cta_primary_label") @db.VarChar(100)
  ctaPrimaryUrl   String?   @map("cta_primary_url") @db.VarChar(1000)
  displayOrder    Int       @default(0) @map("display_order")
  createdAt       DateTime  @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt       DateTime  @updatedAt @map("updated_at") @db.DateTime(3)
  deletedAt       DateTime? @map("deleted_at") @db.DateTime(3)

  experiment  Experiment             @relation(fields: [experimentId], references: [id], onDelete: Cascade)
  exposures   ExperimentExposure[]
  conversions ExperimentConversion[]

  @@unique([experimentId, key])
  @@map("experiment_variants")
}

model ExperimentExposure {
  id           String   @id @default(uuid()) @db.Char(36)
  experimentId String   @map("experiment_id") @db.Char(36)
  variantId    String   @map("variant_id") @db.Char(36)
  anonymousId  String   @map("anonymous_id") @db.VarChar(100)
  createdAt    DateTime @default(now()) @map("created_at") @db.DateTime(3)

  experiment Experiment        @relation(fields: [experimentId], references: [id], onDelete: Cascade)
  variant    ExperimentVariant @relation(fields: [variantId], references: [id], onDelete: Cascade)

  @@index([experimentId, variantId])
  @@map("experiment_exposures")
}

model ExperimentConversion {
  id           String   @id @default(uuid()) @db.Char(36)
  experimentId String   @map("experiment_id") @db.Char(36)
  variantId    String   @map("variant_id") @db.Char(36)
  anonymousId  String   @map("anonymous_id") @db.VarChar(100)
  kind         String   @db.VarChar(50)
  createdAt    DateTime @default(now()) @map("created_at") @db.DateTime(3)

  experiment Experiment        @relation(fields: [experimentId], references: [id], onDelete: Cascade)
  variant    ExperimentVariant @relation(fields: [variantId], references: [id], onDelete: Cascade)

  @@index([experimentId, variantId, kind])
  @@map("experiment_conversions")
}

model NavigationMenu {
  id        String   @id @default(uuid()) @db.Char(36)
  name      String   @db.VarChar(150)
  menuKey   String   @unique @map("menu_key") @db.VarChar(100)
  location  String   @db.VarChar(50)
  status    String   @default("ACTIVE") @db.VarChar(30)
  createdAt DateTime @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt DateTime @updatedAt @map("updated_at") @db.DateTime(3)

  items NavigationItem[]

  @@map("navigation_menus")
}

model NavigationItem {
  id           String   @id @default(uuid()) @db.Char(36)
  menuId       String   @map("menu_id") @db.Char(36)
  parentItemId String?  @map("parent_item_id") @db.Char(36)
  label        String   @db.VarChar(150)
  linkType     String   @map("link_type") @db.VarChar(30)
  pageId       String?  @map("page_id") @db.Char(36)
  customUrl    String?  @map("custom_url") @db.VarChar(1000)
  iconMediaId  String?  @map("icon_media_id") @db.Char(36)
  openInNewTab Boolean  @default(false) @map("open_in_new_tab")
  cssClass     String?  @map("css_class") @db.VarChar(255)
  displayOrder Int      @default(0) @map("display_order")
  status       String   @default("ACTIVE") @db.VarChar(30)
  createdAt    DateTime @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt    DateTime @updatedAt @map("updated_at") @db.DateTime(3)

  menu       NavigationMenu   @relation(fields: [menuId], references: [id], onDelete: Cascade)
  parentItem NavigationItem?  @relation("NavigationItemTree", fields: [parentItemId], references: [id], onDelete: Cascade)
  children   NavigationItem[] @relation("NavigationItemTree")
  page       Page?            @relation(fields: [pageId], references: [id], onDelete: SetNull)
  iconMedia  MediaAsset?      @relation("NavigationItemIconMedia", fields: [iconMediaId], references: [id], onDelete: SetNull)

  @@index([menuId, parentItemId, status, displayOrder])
  @@index([parentItemId])
  @@index([pageId])
  @@index([iconMediaId])
  @@map("navigation_items")
}

model PlatformMetric {
  id              String    @id @default(uuid()) @db.Char(36)
  metricKey       String    @unique @map("metric_key") @db.VarChar(100)
  label           String    @db.VarChar(150)
  numericValue    Decimal?  @map("numeric_value") @db.Decimal(20, 2)
  displayValue    String    @map("display_value") @db.VarChar(100)
  iconMediaId     String?   @map("icon_media_id") @db.Char(36)
  sourceReference String?   @map("source_reference") @db.VarChar(2048)
  verifiedAt      DateTime? @map("verified_at") @db.DateTime(3)
  isVisible       Boolean   @default(true) @map("is_visible")
  displayOrder    Int       @default(0) @map("display_order")
  createdAt       DateTime  @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt       DateTime  @updatedAt @map("updated_at") @db.DateTime(3)

  iconMedia MediaAsset? @relation("PlatformMetricIconMedia", fields: [iconMediaId], references: [id], onDelete: SetNull)

  @@index([iconMediaId])
  @@index([isVisible, displayOrder])
  @@map("platform_metrics")
}

model SeoMetadata {
  id                 String   @id @default(uuid()) @db.Char(36)
  ownerType          String   @map("owner_type") @db.VarChar(50)
  ownerId            String   @map("owner_id") @db.Char(36)
  seoTitle           String   @map("seo_title") @db.VarChar(255)
  metaDescription    String   @map("meta_description") @db.VarChar(500)
  canonicalUrl       String?  @map("canonical_url") @db.VarChar(2048)
  focusKeyword       String?  @map("focus_keyword") @db.VarChar(255)
  ogTitle            String?  @map("og_title") @db.VarChar(255)
  ogDescription      String?  @map("og_description") @db.VarChar(500)
  ogMediaId          String?  @map("og_media_id") @db.Char(36)
  twitterTitle       String?  @map("twitter_title") @db.VarChar(255)
  twitterDescription String?  @map("twitter_description") @db.VarChar(500)
  twitterMediaId     String?  @map("twitter_media_id") @db.Char(36)
  robotsIndex        Boolean  @default(true) @map("robots_index")
  robotsFollow       Boolean  @default(true) @map("robots_follow")
  schemaJson         Json?    @map("schema_json") @db.Json
  hreflangJson       Json?    @map("hreflang_json") @db.Json
  createdAt          DateTime @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt          DateTime @updatedAt @map("updated_at") @db.DateTime(3)

  ogMedia      MediaAsset? @relation("SeoOgMedia", fields: [ogMediaId], references: [id], onDelete: SetNull)
  twitterMedia MediaAsset? @relation("SeoTwitterMedia", fields: [twitterMediaId], references: [id], onDelete: SetNull)

  @@unique([ownerType, ownerId])
  @@index([ogMediaId])
  @@index([twitterMediaId])
  @@map("seo_metadata")
}

/// Reusable, entity-specific metadata rules. These are deliberately resolved
/// at read time: saving a template never rewrites Admin-managed SEO records.
model SeoBulkTemplate {
  id                      String   @id @default(uuid()) @db.Char(36)
  entityType              String   @unique @map("entity_type") @db.VarChar(50)
  seoTitleTemplate        String?  @map("seo_title_template") @db.VarChar(255)
  metaDescriptionTemplate String?  @map("meta_description_template") @db.VarChar(500)
  ogTitleTemplate         String?  @map("og_title_template") @db.VarChar(255)
  ogDescriptionTemplate   String?  @map("og_description_template") @db.VarChar(500)
  canonicalTemplate       String?  @map("canonical_template") @db.VarChar(2048)
  robotsIndex             Boolean? @map("robots_index")
  robotsFollow            Boolean? @map("robots_follow")
  createdAt               DateTime @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt               DateTime @updatedAt @map("updated_at") @db.DateTime(3)

  @@map("seo_bulk_templates")
}

model Redirect {
  id              String    @id @default(uuid()) @db.Char(36)
  sourcePath      String    @map("source_path") @db.VarChar(1000)
  targetPath      String    @map("target_path") @db.VarChar(1000)
  httpStatusCode  Int       @default(301) @map("http_status_code")
  isActive        Boolean   @default(true) @map("is_active")
  hitCount        BigInt    @default(0) @map("hit_count") @db.BigInt
  lastHitAt       DateTime? @map("last_hit_at") @db.DateTime(3)
  createdByUserId String?   @map("created_by_user_id") @db.Char(36)
  createdAt       DateTime  @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt       DateTime  @updatedAt @map("updated_at") @db.DateTime(3)

  createdBy User? @relation("RedirectCreatedBy", fields: [createdByUserId], references: [id], onDelete: SetNull)

  @@unique([sourcePath(length: 750)])
  @@index([createdByUserId])
  @@index([isActive, httpStatusCode])
  @@map("redirects")
}

/// Continents are soft-deleted, so uniqueness has to be scoped to the live
/// rows. `deletedKey` is the discriminator that makes that expressible as a
/// plain unique index: empty while the row is live, the row's own id once it
/// is deleted. Live rows therefore share the key and collide as intended,
/// while every deleted row carries a value nothing else can match — which is
/// what lets an admin recreate a continent they previously deleted.
model Continent {
  id               String    @id @default(uuid()) @db.Char(36)
  name             String    @db.VarChar(150)
  slug             String    @db.VarChar(150)
  code             String?   @db.VarChar(20)
  emoji            String?   @db.VarChar(20)
  shortDescription String?   @map("short_description") @db.VarChar(1000)
  overview         String?   @db.Text
  iconMediaId      String?   @map("icon_media_id") @db.Char(36)
  heroMediaId      String?   @map("hero_media_id") @db.Char(36)
  status           String    @default("ACTIVE") @db.VarChar(30)
  isFeatured       Boolean   @default(false) @map("is_featured")
  displayOrder     Int       @default(0) @map("display_order")
  createdByUserId  String?   @map("created_by_user_id") @db.Char(36)
  updatedByUserId  String?   @map("updated_by_user_id") @db.Char(36)
  createdAt        DateTime  @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt        DateTime  @updatedAt @map("updated_at") @db.DateTime(3)
  deletedAt        DateTime? @map("deleted_at") @db.DateTime(3)
  deletedKey       String    @default("") @map("deleted_key") @db.Char(36)

  iconMedia MediaAsset? @relation("ContinentIconMedia", fields: [iconMediaId], references: [id], onDelete: SetNull)
  heroMedia MediaAsset? @relation("ContinentHeroMedia", fields: [heroMediaId], references: [id], onDelete: SetNull)
  createdBy User?       @relation("ContinentCreatedBy", fields: [createdByUserId], references: [id], onDelete: SetNull)
  updatedBy User?       @relation("ContinentUpdatedBy", fields: [updatedByUserId], references: [id], onDelete: SetNull)
  countries Country[]

  @@unique([name, deletedKey], map: "continents_name_deleted_key")
  @@unique([slug, deletedKey], map: "continents_slug_deleted_key")
  @@unique([code, deletedKey], map: "continents_code_deleted_key")
  @@index([iconMediaId])
  @@index([heroMediaId])
  @@index([createdByUserId])
  @@index([updatedByUserId])
  @@index([status, displayOrder])
  @@map("continents")
}

model Intake {
  id           String   @id @default(uuid()) @db.Char(36)
  name         String   @unique @db.VarChar(100)
  slug         String   @unique @db.VarChar(100)
  startMonth   Int?     @map("start_month") @db.TinyInt
  endMonth     Int?     @map("end_month") @db.TinyInt
  seasonName   String?  @map("season_name") @db.VarChar(50)
  shortLabel   String?  @map("short_label") @db.VarChar(50)
  description  String?  @db.VarChar(500)
  status       String   @default("ACTIVE") @db.VarChar(30)
  displayOrder Int      @default(0) @map("display_order")
  createdAt    DateTime @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt    DateTime @updatedAt @map("updated_at") @db.DateTime(3)

  countryIntakes          CountryIntake[]
  countryCourseIntakes    CountryCourseIntake[]
  leads                   Lead[]
  universityCourseIntakes UniversityCourseIntake[]

  studentPreferences  StudentProfile[]     @relation("StudentPreferredIntake")
  studentApplications StudentApplication[]

  @@index([status, displayOrder])
  @@map("intakes")
}

model Subject {
  id               String    @id @default(uuid()) @db.Char(36)
  name             String    @unique @db.VarChar(255)
  slug             String    @unique @db.VarChar(255)
  shortDescription String?   @map("short_description") @db.VarChar(1000)
  overview         String?   @db.LongText
  iconMediaId      String?   @map("icon_media_id") @db.Char(36)
  listingMediaId   String?   @map("listing_media_id") @db.Char(36)
  heroMediaId      String?   @map("hero_media_id") @db.Char(36)
  status           String    @default("DRAFT") @db.VarChar(30)
  isFeatured       Boolean   @default(false) @map("is_featured")
  displayOrder     Int       @default(0) @map("display_order")
  publishedAt      DateTime? @map("published_at") @db.DateTime(3)
  createdByUserId  String?   @map("created_by_user_id") @db.Char(36)
  updatedByUserId  String?   @map("updated_by_user_id") @db.Char(36)
  createdAt        DateTime  @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt        DateTime  @updatedAt @map("updated_at") @db.DateTime(3)
  deletedAt        DateTime? @map("deleted_at") @db.DateTime(3)

  iconMedia       MediaAsset?      @relation("SubjectIconMedia", fields: [iconMediaId], references: [id], onDelete: SetNull)
  listingMedia    MediaAsset?      @relation("SubjectListingMedia", fields: [listingMediaId], references: [id], onDelete: SetNull)
  heroMedia       MediaAsset?      @relation("SubjectHeroMedia", fields: [heroMediaId], references: [id], onDelete: SetNull)
  createdBy       User?            @relation("SubjectCreatedBy", fields: [createdByUserId], references: [id], onDelete: SetNull)
  updatedBy       User?            @relation("SubjectUpdatedBy", fields: [updatedByUserId], references: [id], onDelete: SetNull)
  subSubjects     SubSubject[]
  courses         Course[]
  countrySubjects CountrySubject[]
  leads           Lead[]

  studentPreferences StudentProfile[] @relation("StudentPreferredSubject")

  @@index([iconMediaId])
  @@index([listingMediaId])
  @@index([heroMediaId])
  @@index([createdByUserId])
  @@index([updatedByUserId])
  @@index([status, isFeatured, displayOrder])
  @@map("subjects")
}

model SubSubject {
  id               String    @id @default(uuid()) @db.Char(36)
  subjectId        String    @map("subject_id") @db.Char(36)
  name             String    @db.VarChar(255)
  slug             String    @unique @db.VarChar(255)
  shortDescription String?   @map("short_description") @db.VarChar(1000)
  overview         String?   @db.LongText
  iconMediaId      String?   @map("icon_media_id") @db.Char(36)
  listingMediaId   String?   @map("listing_media_id") @db.Char(36)
  status           String    @default("DRAFT") @db.VarChar(30)
  isFeatured       Boolean   @default(false) @map("is_featured")
  displayOrder     Int       @default(0) @map("display_order")
  publishedAt      DateTime? @map("published_at") @db.DateTime(3)
  createdByUserId  String?   @map("created_by_user_id") @db.Char(36)
  updatedByUserId  String?   @map("updated_by_user_id") @db.Char(36)
  createdAt        DateTime  @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt        DateTime  @updatedAt @map("updated_at") @db.DateTime(3)
  deletedAt        DateTime? @map("deleted_at") @db.DateTime(3)

  subject      Subject     @relation(fields: [subjectId], references: [id], onDelete: Restrict)
  iconMedia    MediaAsset? @relation("SubSubjectIconMedia", fields: [iconMediaId], references: [id], onDelete: SetNull)
  listingMedia MediaAsset? @relation("SubSubjectListingMedia", fields: [listingMediaId], references: [id], onDelete: SetNull)
  createdBy    User?       @relation("SubSubjectCreatedBy", fields: [createdByUserId], references: [id], onDelete: SetNull)
  updatedBy    User?       @relation("SubSubjectUpdatedBy", fields: [updatedByUserId], references: [id], onDelete: SetNull)
  courses      Course[]

  @@unique([subjectId, name])
  @@index([subjectId, status, displayOrder])
  @@index([iconMediaId])
  @@index([listingMediaId])
  @@index([createdByUserId])
  @@index([updatedByUserId])
  @@map("sub_subjects")
}

/// Country features and accepted English tests were hard-coded lists in the API
/// and a second copy of the same lists in the Admin form, so an operator could
/// not add one without a release. They are master data now, on the same shape as
/// CourseLevel: a stable `code` the stored country configuration and the public
/// filters already match on, plus a label that can be edited.
///
/// `isSystem` marks the eight features and three tests that shipped as
/// constants. They keep working exactly as before and cannot be deleted, which
/// is also what makes the seed idempotent.
model CountryFeature {
  id           String   @id @default(uuid()) @db.Char(36)
  code         String   @unique @db.VarChar(50)
  name         String   @unique @db.VarChar(100)
  status       String   @default("ACTIVE") @db.VarChar(30)
  displayOrder Int      @default(0) @map("display_order")
  isSystem     Boolean  @default(false) @map("is_system")
  createdAt    DateTime @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt    DateTime @updatedAt @map("updated_at") @db.DateTime(3)

  @@map("country_features")
}

model CountryEnglishTest {
  id           String   @id @default(uuid()) @db.Char(36)
  code         String   @unique @db.VarChar(50)
  name         String   @unique @db.VarChar(100)
  status       String   @default("ACTIVE") @db.VarChar(30)
  displayOrder Int      @default(0) @map("display_order")
  isSystem     Boolean  @default(false) @map("is_system")
  createdAt    DateTime @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt    DateTime @updatedAt @map("updated_at") @db.DateTime(3)

  @@map("country_english_tests")
}

model CourseLevel {
  id             String   @id @default(uuid()) @db.Char(36)
  code           String   @unique @db.VarChar(30)
  name           String   @unique @db.VarChar(100)
  description    String?  @db.VarChar(500)
  educationOrder Int      @default(0) @map("education_order")
  status         String   @default("ACTIVE") @db.VarChar(30)
  displayOrder   Int      @default(0) @map("display_order")
  createdAt      DateTime @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt      DateTime @updatedAt @map("updated_at") @db.DateTime(3)

  courses                   Course[]
  leads                     Lead[]
  universityCourseOfferings UniversityCourseOffering[]

  studentPreferences StudentProfile[] @relation("StudentPreferredCourseLevel")

  @@map("course_levels")
}

model StudyMode {
  id           String   @id @default(uuid()) @db.Char(36)
  code         String   @unique @db.VarChar(30)
  name         String   @unique @db.VarChar(100)
  description  String?  @db.VarChar(500)
  status       String   @default("ACTIVE") @db.VarChar(30)
  displayOrder Int      @default(0) @map("display_order")
  createdAt    DateTime @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt    DateTime @updatedAt @map("updated_at") @db.DateTime(3)

  courseStudyModes CourseStudyMode[]

  @@map("study_modes")
}

/// Countries are soft-deleted too, and carried the same permanently-blocked
/// recreation as Continent did. `deletedKey` works exactly as it does there.
model Country {
  id               String    @id @default(uuid()) @db.Char(36)
  continentId      String?   @map("continent_id") @db.Char(36)
  name             String    @db.VarChar(150)
  shortName        String?   @map("short_name") @db.VarChar(100)
  pageHeading      String?   @map("page_heading") @db.VarChar(255)
  slug             String    @db.VarChar(255)
  externalUid      String?   @unique @map("external_uid") @db.VarChar(191)
  iso2Code         String?   @map("iso2_code") @db.Char(2)
  iso3Code         String?   @map("iso3_code") @db.Char(3)
  dialCode         String?   @map("dial_code") @db.VarChar(10)
  capitalCity      String?   @map("capital_city") @db.VarChar(150)
  officialLanguage String?   @map("official_language") @db.VarChar(255)
  nationalityName  String?   @map("nationality_name") @db.VarChar(150)
  currencyName     String?   @map("currency_name") @db.VarChar(100)
  currencyCode     String?   @map("currency_code") @db.Char(3)
  currencySymbol   String?   @map("currency_symbol") @db.VarChar(10)
  timezoneJson     Json?     @map("timezone_json") @db.Json
  flagMediaId      String?   @map("flag_media_id") @db.Char(36)
  listingMediaId   String?   @map("listing_media_id") @db.Char(36)
  heroMediaId      String?   @map("hero_media_id") @db.Char(36)
  mapMediaId       String?   @map("map_media_id") @db.Char(36)
  shortDescription String?   @map("short_description") @db.VarChar(1000)
  overview         String?   @db.LongText
  featuredLabel    String?   @map("featured_label") @db.VarChar(100)
  tagline          String?   @db.VarChar(500)
  isFeatured       Boolean   @default(false) @map("is_featured")
  isPopular        Boolean   @default(false) @map("is_popular")
  status           String    @default("DRAFT") @db.VarChar(30)
  displayOrder     Int       @default(0) @map("display_order")
  publishedAt      DateTime? @map("published_at") @db.DateTime(3)
  lastVerifiedAt   DateTime? @map("last_verified_at") @db.DateTime(3)
  primarySourceUrl String?   @map("primary_source_url") @db.VarChar(2048)
  createdByUserId  String?   @map("created_by_user_id") @db.Char(36)
  updatedByUserId  String?   @map("updated_by_user_id") @db.Char(36)
  createdAt        DateTime  @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt        DateTime  @updatedAt @map("updated_at") @db.DateTime(3)
  deletedAt        DateTime? @map("deleted_at") @db.DateTime(3)
  deletedKey       String    @default("") @map("deleted_key") @db.Char(36)

  continent            Continent?                  @relation(fields: [continentId], references: [id], onDelete: Restrict)
  flagMedia            MediaAsset?                 @relation("CountryFlagMedia", fields: [flagMediaId], references: [id], onDelete: SetNull)
  listingMedia         MediaAsset?                 @relation("CountryListingMedia", fields: [listingMediaId], references: [id], onDelete: SetNull)
  heroMedia            MediaAsset?                 @relation("CountryHeroMedia", fields: [heroMediaId], references: [id], onDelete: SetNull)
  mapMedia             MediaAsset?                 @relation("CountryMapMedia", fields: [mapMediaId], references: [id], onDelete: SetNull)
  createdBy            User?                       @relation("CountryCreatedBy", fields: [createdByUserId], references: [id], onDelete: SetNull)
  updatedBy            User?                       @relation("CountryUpdatedBy", fields: [updatedByUserId], references: [id], onDelete: SetNull)
  aliases              CountryAlias[]
  costProfile          CountryCostProfile?
  workProfile          CountryWorkProfile?
  languageRequirements CountryLanguageRequirement?
  intakes              CountryIntake[]
  statistics           CountryStatistic?
  contentSections      CountryContentSection[]
  faqs                 CountryFaq[]
  tagMaps              CountryTagMap[]
  subjectMaps          CountrySubject[]
  states               State[]
  cities               City[]
  courses              CountryCourse[]
  consultantCards      ConsultantLandingCard[]
  popularUniversities  CountryPopularUniversity[]
  popularCourses       CountryPopularCourse[]
  universities         University[]
  scholarshipCountries ScholarshipCountry[]
  consultantCountries  ConsultantCountry[]
  consultantLocations  ConsultantLocation[]
  successStories       SuccessStory[]
  preferredByLeads     Lead[]                      @relation("LeadPreferredCountry")
  jobs                 Job[]                       @relation("JobCountry")
  events               Event[]                     @relation("EventCountry")

  /// Country-owned editorial configuration. These JSON lists intentionally use
  /// stable codes/numbers so legacy profile columns can remain readable while
  /// new Country editing avoids duplicate institutional facts.
  featureCodes              Json? @map("feature_codes")
  acceptedTests             Json? @map("accepted_tests")
  intakeMonths              Json? @map("intake_months")
  postStudyWorkPermitMonths Int?  @map("post_study_work_permit_months")

  studentNationalities    StudentProfile[]          @relation("StudentNationality")
  studentCurrentResidents StudentProfile[]          @relation("StudentCurrentCountry")
  studentPreferredBy      StudentPreferredCountry[] @relation("StudentPreferredCountry")
  studentAcademicRecords  StudentAcademicRecord[]   @relation("StudentAcademicCountry")
  studentPassports        StudentPassport[]         @relation("StudentPassportCountry")

  @@unique([name, deletedKey], map: "countries_name_deleted_key")
  @@unique([slug, deletedKey], map: "countries_slug_deleted_key")
  @@unique([iso2Code, deletedKey], map: "countries_iso2_deleted_key")
  @@unique([iso3Code, deletedKey], map: "countries_iso3_deleted_key")
  @@index([continentId, status, displayOrder])
  @@index([externalUid])
  @@index([status, isFeatured, isPopular])
  @@index([flagMediaId])
  @@index([listingMediaId])
  @@index([heroMediaId])
  @@index([mapMediaId])
  @@index([createdByUserId])
  @@index([updatedByUserId])
  @@fulltext([name, pageHeading, shortDescription])
  @@map("countries")
}

model CountryAlias {
  id              String   @id @default(uuid()) @db.Char(36)
  countryId       String   @map("country_id") @db.Char(36)
  alias           String   @db.VarChar(255)
  normalizedAlias String   @map("normalized_alias") @db.VarChar(255)
  aliasType       String   @default("COMMON") @map("alias_type") @db.VarChar(30)
  locale          String   @default("en") @db.VarChar(20)
  createdAt       DateTime @default(now()) @map("created_at") @db.DateTime(3)

  country Country @relation(fields: [countryId], references: [id], onDelete: Cascade)

  @@unique([countryId, normalizedAlias])
  @@index([countryId])
  @@index([normalizedAlias])
  @@map("country_aliases")
}

model CountryCostProfile {
  id                  String    @id @default(uuid()) @db.Char(36)
  countryId           String    @unique @map("country_id") @db.Char(36)
  currencyCode        String    @map("currency_code") @db.Char(3)
  currencySymbol      String?   @map("currency_symbol") @db.VarChar(10)
  tuitionMin          Decimal?  @map("tuition_min") @db.Decimal(12, 2)
  tuitionMax          Decimal?  @map("tuition_max") @db.Decimal(12, 2)
  tuitionPeriod       String    @default("PER_YEAR") @map("tuition_period") @db.VarChar(30)
  tuitionNotes        String?   @map("tuition_notes") @db.Text
  livingCostMin       Decimal?  @map("living_cost_min") @db.Decimal(12, 2)
  livingCostMax       Decimal?  @map("living_cost_max") @db.Decimal(12, 2)
  livingCostPeriod    String    @default("PER_MONTH") @map("living_cost_period") @db.VarChar(30)
  livingCostNotes     String?   @map("living_cost_notes") @db.Text
  accommodationMin    Decimal?  @map("accommodation_min") @db.Decimal(12, 2)
  accommodationMax    Decimal?  @map("accommodation_max") @db.Decimal(12, 2)
  foodCostMin         Decimal?  @map("food_cost_min") @db.Decimal(12, 2)
  foodCostMax         Decimal?  @map("food_cost_max") @db.Decimal(12, 2)
  transportCostMin    Decimal?  @map("transport_cost_min") @db.Decimal(12, 2)
  transportCostMax    Decimal?  @map("transport_cost_max") @db.Decimal(12, 2)
  healthInsuranceCost Decimal?  @map("health_insurance_cost") @db.Decimal(12, 2)
  applicationFeeMin   Decimal?  @map("application_fee_min") @db.Decimal(12, 2)
  applicationFeeMax   Decimal?  @map("application_fee_max") @db.Decimal(12, 2)
  budgetBand          String?   @map("budget_band") @db.VarChar(30)
  applicableYear      Int?      @map("applicable_year") @db.SmallInt
  sourceReference     String?   @map("source_reference") @db.VarChar(2048)
  disclaimer          String?   @db.Text
  verifiedAt          DateTime? @map("verified_at") @db.DateTime(3)
  createdAt           DateTime  @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt           DateTime  @updatedAt @map("updated_at") @db.DateTime(3)

  country Country @relation(fields: [countryId], references: [id], onDelete: Cascade)

  @@index([budgetBand, applicableYear])
  @@map("country_cost_profiles")
}

model CountryWorkProfile {
  id                         String    @id @default(uuid()) @db.Char(36)
  countryId                  String    @unique @map("country_id") @db.Char(36)
  partTimeAllowed            Boolean   @default(false) @map("part_time_allowed")
  partTimeHoursPerWeek       Decimal?  @map("part_time_hours_per_week") @db.Decimal(5, 2)
  partTimeHoursDuringBreaks  Decimal?  @map("part_time_hours_during_breaks") @db.Decimal(5, 2)
  partTimeSummary            String?   @map("part_time_summary") @db.Text
  postStudyWorkAvailable     Boolean   @default(false) @map("post_study_work_available")
  postStudyWorkMinMonths     Int?      @map("post_study_work_min_months")
  postStudyWorkMaxMonths     Int?      @map("post_study_work_max_months")
  postStudyWorkSummary       String?   @map("post_study_work_summary") @db.Text
  immigrationPathwayStrength String?   @map("immigration_pathway_strength") @db.VarChar(30)
  immigrationPathwaySummary  String?   @map("immigration_pathway_summary") @db.Text
  visaSuccessBand            String    @default("NOT_PUBLISHED") @map("visa_success_band") @db.VarChar(30)
  visaSuccessPercentage      Decimal?  @map("visa_success_percentage") @db.Decimal(5, 2)
  visaInformation            String?   @map("visa_information") @db.LongText
  visaType                   String?   @map("visa_type") @db.VarChar(255)
  visaFee                    Decimal?  @map("visa_fee") @db.Decimal(12, 2)
  visaFeeCurrencyCode        String?   @map("visa_fee_currency_code") @db.Char(3)
  visaProcessingTime         String?   @map("visa_processing_time") @db.VarChar(255)
  proofOfFundsSummary        String?   @map("proof_of_funds_summary") @db.Text
  sourceReference            String?   @map("source_reference") @db.VarChar(2048)
  disclaimer                 String?   @db.Text
  verifiedAt                 DateTime? @map("verified_at") @db.DateTime(3)
  createdAt                  DateTime  @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt                  DateTime  @updatedAt @map("updated_at") @db.DateTime(3)

  country Country @relation(fields: [countryId], references: [id], onDelete: Cascade)

  @@index([partTimeAllowed, postStudyWorkAvailable])
  @@index([immigrationPathwayStrength, visaSuccessBand])
  @@map("country_work_profiles")
}

model CountryLanguageRequirement {
  id                      String    @id @default(uuid()) @db.Char(36)
  countryId               String    @unique @map("country_id") @db.Char(36)
  ieltsRequirement        String    @default("VARIES") @map("ielts_requirement") @db.VarChar(30)
  ieltsMinScore           Decimal?  @map("ielts_min_score") @db.Decimal(3, 1)
  ieltsNotes              String?   @map("ielts_notes") @db.VarChar(500)
  pteRequirement          String    @default("VARIES") @map("pte_requirement") @db.VarChar(30)
  pteMinScore             Decimal?  @map("pte_min_score") @db.Decimal(5, 2)
  pteNotes                String?   @map("pte_notes") @db.VarChar(500)
  toeflRequirement        String    @default("VARIES") @map("toefl_requirement") @db.VarChar(30)
  toeflMinScore           Decimal?  @map("toefl_min_score") @db.Decimal(5, 2)
  toeflNotes              String?   @map("toefl_notes") @db.VarChar(500)
  duolingoRequirement     String    @default("VARIES") @map("duolingo_requirement") @db.VarChar(30)
  duolingoMinScore        Decimal?  @map("duolingo_min_score") @db.Decimal(5, 2)
  duolingoNotes           String?   @map("duolingo_notes") @db.VarChar(500)
  languageWaiverAvailable Boolean   @default(false) @map("language_waiver_available")
  waiverNotes             String?   @map("waiver_notes") @db.Text
  generalNotes            String?   @map("general_notes") @db.Text
  sourceReference         String?   @map("source_reference") @db.VarChar(2048)
  disclaimer              String?   @db.Text
  verifiedAt              DateTime? @map("verified_at") @db.DateTime(3)
  createdAt               DateTime  @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt               DateTime  @updatedAt @map("updated_at") @db.DateTime(3)

  country Country @relation(fields: [countryId], references: [id], onDelete: Cascade)

  @@index([ieltsRequirement, languageWaiverAvailable])
  @@map("country_language_requirements")
}

model CountryIntake {
  id                       String   @id @default(uuid()) @db.Char(36)
  countryId                String   @map("country_id") @db.Char(36)
  intakeId                 String   @map("intake_id") @db.Char(36)
  isMajor                  Boolean  @default(false) @map("is_major")
  availabilityStatus       String   @default("AVAILABLE") @map("availability_status") @db.VarChar(30)
  applicationOpeningMonth  Int?     @map("application_opening_month") @db.TinyInt
  applicationDeadlineMonth Int?     @map("application_deadline_month") @db.TinyInt
  applicationOpeningNote   String?  @map("application_opening_note") @db.VarChar(500)
  applicationDeadlineNote  String?  @map("application_deadline_note") @db.VarChar(500)
  notes                    String?  @db.Text
  displayOrder             Int      @default(0) @map("display_order")
  createdAt                DateTime @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt                DateTime @updatedAt @map("updated_at") @db.DateTime(3)

  country Country @relation(fields: [countryId], references: [id], onDelete: Cascade)
  intake  Intake  @relation(fields: [intakeId], references: [id], onDelete: Restrict)

  @@unique([countryId, intakeId])
  @@index([countryId])
  @@index([intakeId, isMajor, availabilityStatus])
  @@map("country_intakes")
}

model CountryStatistic {
  id                            String    @id @default(uuid()) @db.Char(36)
  countryId                     String    @unique @map("country_id") @db.Char(36)
  universitiesCount             Int       @default(0) @map("universities_count")
  publicUniversitiesCount       Int       @default(0) @map("public_universities_count")
  privateUniversitiesCount      Int       @default(0) @map("private_universities_count")
  coursesCount                  Int       @default(0) @map("courses_count")
  ugCoursesCount                Int       @default(0) @map("ug_courses_count")
  pgCoursesCount                Int       @default(0) @map("pg_courses_count")
  pgdmCoursesCount              Int       @default(0) @map("pgdm_courses_count")
  mbaCoursesCount               Int       @default(0) @map("mba_courses_count")
  phdCoursesCount               Int       @default(0) @map("phd_courses_count")
  scholarshipsCount             Int       @default(0) @map("scholarships_count")
  citiesCount                   Int       @default(0) @map("cities_count")
  topRankedUniversitiesCount    Int       @default(0) @map("top_ranked_universities_count")
  internationalStudentsCount    Int?      @map("international_students_count")
  studentSatisfactionPercentage Decimal?  @map("student_satisfaction_percentage") @db.Decimal(5, 2)
  sourceMode                    String    @default("DERIVED") @map("source_mode") @db.VarChar(30)
  sourceReference               String?   @map("source_reference") @db.VarChar(2048)
  verifiedAt                    DateTime? @map("verified_at") @db.DateTime(3)
  createdAt                     DateTime  @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt                     DateTime  @updatedAt @map("updated_at") @db.DateTime(3)

  country Country @relation(fields: [countryId], references: [id], onDelete: Cascade)

  @@map("country_statistics")
}

model CountryContentSection {
  id                String    @id @default(uuid()) @db.Char(36)
  countryId         String    @map("country_id") @db.Char(36)
  sectionKey        String    @map("section_key") @db.VarChar(100)
  sectionType       String    @default("RICH_TEXT") @map("section_type") @db.VarChar(50)
  eyebrow           String?   @db.VarChar(255)
  heading           String?   @db.VarChar(500)
  subheading        String?   @db.Text
  bodyJson          Json?     @map("body_json") @db.Json
  primaryMediaId    String?   @map("primary_media_id") @db.Char(36)
  secondaryMediaId  String?   @map("secondary_media_id") @db.Char(36)
  ctaLabel          String?   @map("cta_label") @db.VarChar(100)
  ctaUrl            String?   @map("cta_url") @db.VarChar(1000)
  configurationJson Json?     @map("configuration_json") @db.Json
  displayOrder      Int       @default(0) @map("display_order")
  status            String    @default("ACTIVE") @db.VarChar(30)
  createdAt         DateTime  @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt         DateTime  @updatedAt @map("updated_at") @db.DateTime(3)
  deletedAt         DateTime? @map("deleted_at") @db.DateTime(3)

  country        Country     @relation(fields: [countryId], references: [id], onDelete: Cascade)
  primaryMedia   MediaAsset? @relation("CountryPrimaryMedia", fields: [primaryMediaId], references: [id], onDelete: SetNull)
  secondaryMedia MediaAsset? @relation("CountrySecondaryMedia", fields: [secondaryMediaId], references: [id], onDelete: SetNull)

  @@unique([countryId, sectionKey])
  @@index([countryId, status, displayOrder])
  @@index([primaryMediaId])
  @@index([secondaryMediaId])
  @@map("country_content_sections")
}

model CountryFaq {
  id              String    @id @default(uuid()) @db.Char(36)
  countryId       String    @map("country_id") @db.Char(36)
  question        String    @db.VarChar(1000)
  answer          String    @db.LongText
  category        String?   @db.VarChar(100)
  isFeatured      Boolean   @default(false) @map("is_featured")
  status          String    @default("ACTIVE") @db.VarChar(30)
  displayOrder    Int       @default(0) @map("display_order")
  createdByUserId String?   @map("created_by_user_id") @db.Char(36)
  updatedByUserId String?   @map("updated_by_user_id") @db.Char(36)
  createdAt       DateTime  @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt       DateTime  @updatedAt @map("updated_at") @db.DateTime(3)
  deletedAt       DateTime? @map("deleted_at") @db.DateTime(3)

  country   Country @relation(fields: [countryId], references: [id], onDelete: Cascade)
  createdBy User?   @relation("CountryFaqCreatedBy", fields: [createdByUserId], references: [id], onDelete: SetNull)
  updatedBy User?   @relation("CountryFaqUpdatedBy", fields: [updatedByUserId], references: [id], onDelete: SetNull)

  @@index([countryId, status, displayOrder])
  @@index([createdByUserId])
  @@index([updatedByUserId])
  @@map("country_faqs")
}

model CountryTag {
  id          String   @id @default(uuid()) @db.Char(36)
  name        String   @unique @db.VarChar(100)
  slug        String   @unique @db.VarChar(100)
  tagType     String   @default("GENERAL") @map("tag_type") @db.VarChar(50)
  description String?  @db.VarChar(500)
  status      String   @default("ACTIVE") @db.VarChar(30)
  createdAt   DateTime @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt   DateTime @updatedAt @map("updated_at") @db.DateTime(3)

  tagMaps CountryTagMap[]

  @@map("country_tags")
}

model CountryTagMap {
  id        String   @id @default(uuid()) @db.Char(36)
  countryId String   @map("country_id") @db.Char(36)
  tagId     String   @map("tag_id") @db.Char(36)
  createdAt DateTime @default(now()) @map("created_at") @db.DateTime(3)

  country Country    @relation(fields: [countryId], references: [id], onDelete: Cascade)
  tag     CountryTag @relation(fields: [tagId], references: [id], onDelete: Cascade)

  @@unique([countryId, tagId])
  @@index([countryId])
  @@index([tagId, countryId])
  @@map("country_tag_map")
}

model CountrySubject {
  id           String   @id @default(uuid()) @db.Char(36)
  countryId    String   @map("country_id") @db.Char(36)
  subjectId    String   @map("subject_id") @db.Char(36)
  displayOrder Int      @default(0) @map("display_order")
  createdAt    DateTime @default(now()) @map("created_at") @db.DateTime(3)

  country Country @relation(fields: [countryId], references: [id], onDelete: Cascade)
  subject Subject @relation(fields: [subjectId], references: [id], onDelete: Cascade)

  @@unique([countryId, subjectId])
  @@index([countryId, displayOrder])
  @@index([subjectId, countryId])
  @@map("country_subjects")
}

model State {
  id           String    @id @default(uuid()) @db.Char(36)
  countryId    String    @map("country_id") @db.Char(36)
  name         String    @db.VarChar(150)
  slug         String    @db.VarChar(160)
  status       String    @default("DRAFT") @db.VarChar(30)
  displayOrder Int       @default(0) @map("display_order")
  createdAt    DateTime  @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt    DateTime  @updatedAt @map("updated_at") @db.DateTime(3)
  deletedAt    DateTime? @map("deleted_at") @db.DateTime(3)

  country             Country              @relation(fields: [countryId], references: [id], onDelete: Cascade)
  cities              City[]
  campuses            UniversityCampus[]   @relation("CampusState")
  consultantLocations ConsultantLocation[] @relation("ConsultantLocationState")
  jobs                Job[]                @relation("JobState")
  events              Event[]              @relation("EventState")

  @@unique([countryId, slug])
  @@index([countryId, status, displayOrder])
  @@map("states")
}

model City {
  id               String    @id @default(uuid()) @db.Char(36)
  countryId        String    @map("country_id") @db.Char(36)
  stateId          String?   @map("state_id") @db.Char(36)
  name             String    @db.VarChar(150)
  slug             String    @db.VarChar(160)
  shortDescription String?   @map("short_description") @db.VarChar(1000)
  overview         String?   @db.LongText
  heroMediaId      String?   @map("hero_media_id") @db.Char(36)
  isFeatured       Boolean   @default(false) @map("is_featured")
  status           String    @default("DRAFT") @db.VarChar(30)
  publishStartsAt  DateTime? @map("publish_starts_at") @db.DateTime(3)
  publishEndsAt    DateTime? @map("publish_ends_at") @db.DateTime(3)
  displayOrder     Int       @default(0) @map("display_order")
  publishedAt      DateTime? @map("published_at") @db.DateTime(3)
  createdByUserId  String?   @map("created_by_user_id") @db.Char(36)
  updatedByUserId  String?   @map("updated_by_user_id") @db.Char(36)
  createdAt        DateTime  @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt        DateTime  @updatedAt @map("updated_at") @db.DateTime(3)
  deletedAt        DateTime? @map("deleted_at") @db.DateTime(3)

  country             Country              @relation(fields: [countryId], references: [id], onDelete: Cascade)
  state               State?               @relation(fields: [stateId], references: [id], onDelete: SetNull)
  heroMedia           MediaAsset?          @relation("CityHeroMedia", fields: [heroMediaId], references: [id], onDelete: SetNull)
  createdBy           User?                @relation("CityCreatedBy", fields: [createdByUserId], references: [id], onDelete: SetNull)
  updatedBy           User?                @relation("CityUpdatedBy", fields: [updatedByUserId], references: [id], onDelete: SetNull)
  campuses            UniversityCampus[]   @relation("CampusCity")
  consultantLocations ConsultantLocation[] @relation("ConsultantLocationCity")
  jobs                Job[]                @relation("JobCity")
  events              Event[]              @relation("EventCity")

  @@unique([countryId, slug])
  @@index([countryId, status, displayOrder])
  @@index([stateId])
  @@index([heroMediaId])
  @@map("cities")
}

model Course {
  id                    String    @id @default(uuid()) @db.Char(36)
  subjectId             String    @map("subject_id") @db.Char(36)
  subSubjectId          String?   @map("sub_subject_id") @db.Char(36)
  courseLevelId         String    @map("course_level_id") @db.Char(36)
  name                  String    @db.VarChar(255)
  shortName             String?   @map("short_name") @db.VarChar(150)
  qualificationName     String?   @map("qualification_name") @db.VarChar(255)
  slug                  String    @unique @db.VarChar(255)
  courseCode            String?   @map("course_code") @db.VarChar(100)
  shortDescription      String?   @map("short_description") @db.VarChar(1000)
  overview              String?   @db.LongText
  durationMin           Decimal?  @map("duration_min") @db.Decimal(6, 2)
  durationMax           Decimal?  @map("duration_max") @db.Decimal(6, 2)
  durationUnit          String?   @map("duration_unit") @db.VarChar(30)
  credits               Decimal?  @db.Decimal(8, 2)
  featuredMediaId       String?   @map("featured_media_id") @db.Char(36)
  careerSummary         String?   @map("career_summary") @db.Text
  averageStartingSalary Decimal?  @map("average_starting_salary") @db.Decimal(12, 2)
  salaryCurrencyCode    String?   @map("salary_currency_code") @db.Char(3)
  status                String    @default("DRAFT") @db.VarChar(30)
  isFeatured            Boolean   @default(false) @map("is_featured")
  featuredPriority      Int       @default(0) @map("featured_priority")
  featuredFrom          DateTime? @map("featured_from") @db.DateTime(3)
  featuredUntil         DateTime? @map("featured_until") @db.DateTime(3)
  publishStartsAt       DateTime? @map("publish_starts_at") @db.DateTime(3)
  publishEndsAt         DateTime? @map("publish_ends_at") @db.DateTime(3)
  popularityScore       Decimal   @default(0) @map("popularity_score") @db.Decimal(10, 2)
  displayOrder          Int       @default(0) @map("display_order")
  publishedAt           DateTime? @map("published_at") @db.DateTime(3)
  createdByUserId       String?   @map("created_by_user_id") @db.Char(36)
  updatedByUserId       String?   @map("updated_by_user_id") @db.Char(36)
  createdAt             DateTime  @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt             DateTime  @updatedAt @map("updated_at") @db.DateTime(3)
  deletedAt             DateTime? @map("deleted_at") @db.DateTime(3)

  subject             Subject                    @relation(fields: [subjectId], references: [id], onDelete: Restrict)
  subSubject          SubSubject?                @relation(fields: [subSubjectId], references: [id], onDelete: SetNull)
  courseLevel         CourseLevel                @relation(fields: [courseLevelId], references: [id], onDelete: Restrict)
  featuredMedia       MediaAsset?                @relation("CourseFeaturedMedia", fields: [featuredMediaId], references: [id], onDelete: SetNull)
  createdBy           User?                      @relation("CourseCreatedBy", fields: [createdByUserId], references: [id], onDelete: SetNull)
  updatedBy           User?                      @relation("CourseUpdatedBy", fields: [updatedByUserId], references: [id], onDelete: SetNull)
  studyModes          CourseStudyMode[]
  countryCourses      CountryCourse[]
  contentSections     CourseContentSection[]
  faqs                CourseFaq[]
  relatedFrom         RelatedCourse[]            @relation("RelatedCourseSource")
  relatedTo           RelatedCourse[]            @relation("RelatedCourseTarget")
  preferredByLeads    Lead[]                     @relation("LeadPreferredCourse")
  universityOfferings UniversityCourseOffering[]
  popularInCountries  CountryPopularCourse[]

  @@index([subjectId, subSubjectId, courseLevelId, status])
  @@index([courseLevelId])
  @@index([subSubjectId])
  @@index([featuredMediaId])
  @@index([createdByUserId])
  @@index([updatedByUserId])
  @@index([status, isFeatured, popularityScore])
  @@fulltext([name, shortDescription, overview])
  @@map("courses")
}

model CourseStudyMode {
  id          String   @id @default(uuid()) @db.Char(36)
  courseId    String   @map("course_id") @db.Char(36)
  studyModeId String   @map("study_mode_id") @db.Char(36)
  createdAt   DateTime @default(now()) @map("created_at") @db.DateTime(3)

  course    Course    @relation(fields: [courseId], references: [id], onDelete: Cascade)
  studyMode StudyMode @relation(fields: [studyModeId], references: [id], onDelete: Restrict)

  @@unique([courseId, studyModeId])
  @@index([courseId])
  @@index([studyModeId])
  @@map("course_study_modes")
}

model CountryCourse {
  id                    String    @id @default(uuid()) @db.Char(36)
  countryId             String    @map("country_id") @db.Char(36)
  courseId              String    @map("course_id") @db.Char(36)
  availabilityStatus    String    @default("AVAILABLE") @map("availability_status") @db.VarChar(30)
  indicativeTuitionMin  Decimal?  @map("indicative_tuition_min") @db.Decimal(12, 2)
  indicativeTuitionMax  Decimal?  @map("indicative_tuition_max") @db.Decimal(12, 2)
  currencyCode          String?   @map("currency_code") @db.Char(3)
  tuitionPeriod         String    @default("PER_YEAR") @map("tuition_period") @db.VarChar(30)
  applicationFeeMin     Decimal?  @map("application_fee_min") @db.Decimal(12, 2)
  applicationFeeMax     Decimal?  @map("application_fee_max") @db.Decimal(12, 2)
  durationMinOverride   Decimal?  @map("duration_min_override") @db.Decimal(6, 2)
  durationMaxOverride   Decimal?  @map("duration_max_override") @db.Decimal(6, 2)
  durationUnitOverride  String?   @map("duration_unit_override") @db.VarChar(30)
  academicMinPercentage Decimal?  @map("academic_min_percentage") @db.Decimal(5, 2)
  academicMinCgpa       Decimal?  @map("academic_min_cgpa") @db.Decimal(4, 2)
  ieltsMinScore         Decimal?  @map("ielts_min_score") @db.Decimal(3, 1)
  pteMinScore           Decimal?  @map("pte_min_score") @db.Decimal(5, 2)
  toeflMinScore         Decimal?  @map("toefl_min_score") @db.Decimal(5, 2)
  duolingoMinScore      Decimal?  @map("duolingo_min_score") @db.Decimal(5, 2)
  workExperienceMonths  Int?      @map("work_experience_months")
  scholarshipAvailable  Boolean   @default(false) @map("scholarship_available")
  admissionRequirements String?   @map("admission_requirements") @db.LongText
  englishRequirements   String?   @map("english_requirements") @db.LongText
  applicationNotes      String?   @map("application_notes") @db.Text
  careerOpportunities   String?   @map("career_opportunities") @db.Text
  sourceReference       String?   @map("source_reference") @db.VarChar(2048)
  verifiedAt            DateTime? @map("verified_at") @db.DateTime(3)
  status                String    @default("ACTIVE") @db.VarChar(30)
  isFeatured            Boolean   @default(false) @map("is_featured")
  displayOrder          Int       @default(0) @map("display_order")
  createdAt             DateTime  @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt             DateTime  @updatedAt @map("updated_at") @db.DateTime(3)
  deletedAt             DateTime? @map("deleted_at") @db.DateTime(3)

  country Country               @relation(fields: [countryId], references: [id], onDelete: Restrict)
  course  Course                @relation(fields: [courseId], references: [id], onDelete: Restrict)
  intakes CountryCourseIntake[]

  @@unique([countryId, courseId])
  @@index([countryId, status, isFeatured])
  @@index([courseId, status])
  @@map("country_courses")
}

model CountryCourseIntake {
  id                  String    @id @default(uuid()) @db.Char(36)
  countryCourseId     String    @map("country_course_id") @db.Char(36)
  intakeId            String    @map("intake_id") @db.Char(36)
  applicationDeadline DateTime? @map("application_deadline") @db.Date
  deadlineNotes       String?   @map("deadline_notes") @db.VarChar(500)
  status              String    @default("ACTIVE") @db.VarChar(30)
  createdAt           DateTime  @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt           DateTime  @updatedAt @map("updated_at") @db.DateTime(3)

  countryCourse CountryCourse @relation(fields: [countryCourseId], references: [id], onDelete: Cascade)
  intake        Intake        @relation(fields: [intakeId], references: [id], onDelete: Restrict)

  @@unique([countryCourseId, intakeId])
  @@index([countryCourseId])
  @@index([intakeId, status])
  @@map("country_course_intakes")
}

model CourseContentSection {
  id           String    @id @default(uuid()) @db.Char(36)
  courseId     String    @map("course_id") @db.Char(36)
  sectionKey   String    @map("section_key") @db.VarChar(100)
  heading      String?   @db.VarChar(500)
  subheading   String?   @db.Text
  bodyJson     Json?     @map("body_json") @db.Json
  mediaId      String?   @map("media_id") @db.Char(36)
  displayOrder Int       @default(0) @map("display_order")
  status       String    @default("ACTIVE") @db.VarChar(30)
  createdAt    DateTime  @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt    DateTime  @updatedAt @map("updated_at") @db.DateTime(3)
  deletedAt    DateTime? @map("deleted_at") @db.DateTime(3)

  course Course      @relation(fields: [courseId], references: [id], onDelete: Cascade)
  media  MediaAsset? @relation("CourseSectionMedia", fields: [mediaId], references: [id], onDelete: SetNull)

  @@unique([courseId, sectionKey])
  @@index([courseId, status, displayOrder])
  @@index([mediaId])
  @@map("course_content_sections")
}

model CourseFaq {
  id           String    @id @default(uuid()) @db.Char(36)
  courseId     String    @map("course_id") @db.Char(36)
  question     String    @db.VarChar(1000)
  answer       String    @db.LongText
  status       String    @default("ACTIVE") @db.VarChar(30)
  displayOrder Int       @default(0) @map("display_order")
  createdAt    DateTime  @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt    DateTime  @updatedAt @map("updated_at") @db.DateTime(3)
  deletedAt    DateTime? @map("deleted_at") @db.DateTime(3)

  course Course @relation(fields: [courseId], references: [id], onDelete: Cascade)

  @@index([courseId, status, displayOrder])
  @@map("course_faqs")
}

model RelatedCourse {
  id               String   @id @default(uuid()) @db.Char(36)
  courseId         String   @map("course_id") @db.Char(36)
  relatedCourseId  String   @map("related_course_id") @db.Char(36)
  relationshipType String   @default("RELATED") @map("relationship_type") @db.VarChar(30)
  displayOrder     Int      @default(0) @map("display_order")
  createdAt        DateTime @default(now()) @map("created_at") @db.DateTime(3)

  course        Course @relation("RelatedCourseSource", fields: [courseId], references: [id], onDelete: Restrict)
  relatedCourse Course @relation("RelatedCourseTarget", fields: [relatedCourseId], references: [id], onDelete: Restrict)

  @@unique([courseId, relatedCourseId, relationshipType])
  @@index([courseId])
  @@index([relatedCourseId])
  @@map("related_courses")
}

model ConsultantLandingCard {
  id                 String    @id @default(uuid()) @db.Char(36)
  countryId          String?   @map("country_id") @db.Char(36)
  title              String    @db.VarChar(255)
  slug               String    @unique @db.VarChar(255)
  shortDescription   String    @map("short_description") @db.VarChar(1000)
  overview           String?   @db.LongText
  iconMediaId        String?   @map("icon_media_id") @db.Char(36)
  featuredMediaId    String?   @map("featured_media_id") @db.Char(36)
  isFreeConsultation Boolean   @default(true) @map("is_free_consultation")
  ctaLabel           String    @default("View consultants") @map("cta_label") @db.VarChar(100)
  ctaUrl             String?   @map("cta_url") @db.VarChar(1000)
  status             String    @default("DRAFT") @db.VarChar(30)
  isFeatured         Boolean   @default(false) @map("is_featured")
  displayOrder       Int       @default(0) @map("display_order")
  publishedAt        DateTime? @map("published_at") @db.DateTime(3)
  createdAt          DateTime  @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt          DateTime  @updatedAt @map("updated_at") @db.DateTime(3)
  deletedAt          DateTime? @map("deleted_at") @db.DateTime(3)

  country       Country?    @relation(fields: [countryId], references: [id], onDelete: SetNull)
  iconMedia     MediaAsset? @relation("ConsultantIconMedia", fields: [iconMediaId], references: [id], onDelete: SetNull)
  featuredMedia MediaAsset? @relation("ConsultantFeaturedMedia", fields: [featuredMediaId], references: [id], onDelete: SetNull)

  @@index([countryId, status, displayOrder])
  @@index([iconMediaId])
  @@index([featuredMediaId])
  @@map("consultant_landing_cards")
}

model Lead {
  id                     String    @id @default(uuid()) @db.Char(36)
  leadNumber             String    @unique @map("lead_number") @db.VarChar(50)
  formType               String    @map("form_type") @db.VarChar(50)
  sourceType             String?   @map("source_type") @db.VarChar(50)
  sourceEntityId         String?   @map("source_entity_id") @db.Char(36)
  sourcePageUrl          String?   @map("source_page_url") @db.VarChar(2048)
  firstName              String    @map("first_name") @db.VarChar(100)
  lastName               String?   @map("last_name") @db.VarChar(100)
  email                  String?   @db.VarChar(255)
  phoneCountryCode       String?   @map("phone_country_code") @db.VarChar(10)
  phoneNumber            String    @map("phone_number") @db.VarChar(30)
  currentCity            String?   @map("current_city") @db.VarChar(150)
  currentState           String?   @map("current_state") @db.VarChar(150)
  currentCountry         String?   @map("current_country") @db.VarChar(150)
  preferredCountryId     String?   @map("preferred_country_id") @db.Char(36)
  preferredCourseId      String?   @map("preferred_course_id") @db.Char(36)
  preferredSubjectId     String?   @map("preferred_subject_id") @db.Char(36)
  preferredCourseLevelId String?   @map("preferred_course_level_id") @db.Char(36)
  preferredIntakeId      String?   @map("preferred_intake_id") @db.Char(36)
  budgetMin              Decimal?  @map("budget_min") @db.Decimal(12, 2)
  budgetMax              Decimal?  @map("budget_max") @db.Decimal(12, 2)
  budgetCurrencyCode     String?   @map("budget_currency_code") @db.Char(3)
  englishTestType        String?   @map("english_test_type") @db.VarChar(30)
  englishTestScore       Decimal?  @map("english_test_score") @db.Decimal(6, 2)
  highestQualification   String?   @map("highest_qualification") @db.VarChar(255)
  message                String?   @db.Text
  status                 String    @default("NEW") @db.VarChar(30)
  priority               String    @default("NORMAL") @db.VarChar(30)
  assignedToUserId       String?   @map("assigned_to_user_id") @db.Char(36)
  nextFollowUpAt         DateTime? @map("next_follow_up_at") @db.DateTime(3)
  marketingConsent       Boolean   @default(false) @map("marketing_consent")
  privacyConsent         Boolean   @default(false) @map("privacy_consent")
  utmSource              String?   @map("utm_source") @db.VarChar(255)
  utmMedium              String?   @map("utm_medium") @db.VarChar(255)
  utmCampaign            String?   @map("utm_campaign") @db.VarChar(255)
  utmTerm                String?   @map("utm_term") @db.VarChar(255)
  utmContent             String?   @map("utm_content") @db.VarChar(255)
  referrerUrl            String?   @map("referrer_url") @db.VarChar(2048)
  landingPageUrl         String?   @map("landing_page_url") @db.VarChar(2048)
  ipAddress              String?   @map("ip_address") @db.VarChar(45)
  userAgent              String?   @map("user_agent") @db.VarChar(1000)
  createdAt              DateTime  @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt              DateTime  @updatedAt @map("updated_at") @db.DateTime(3)
  deletedAt              DateTime? @map("deleted_at") @db.DateTime(3)

  preferredCountry     Country?             @relation("LeadPreferredCountry", fields: [preferredCountryId], references: [id], onDelete: SetNull)
  preferredCourse      Course?              @relation("LeadPreferredCourse", fields: [preferredCourseId], references: [id], onDelete: SetNull)
  preferredSubject     Subject?             @relation(fields: [preferredSubjectId], references: [id], onDelete: SetNull)
  preferredCourseLevel CourseLevel?         @relation(fields: [preferredCourseLevelId], references: [id], onDelete: SetNull)
  preferredIntake      Intake?              @relation(fields: [preferredIntakeId], references: [id], onDelete: SetNull)
  assignedTo           User?                @relation("LeadAssignedTo", fields: [assignedToUserId], references: [id], onDelete: SetNull)
  bookings             CounsellingBooking[]
  notes                LeadNote[]
  statusHistory        LeadStatusHistory[]
  contactInquiry       ContactInquiry?      @relation("ContactInquiryConvertedLead")

  @@index([status, createdAt])
  @@index([phoneNumber])
  @@index([email])
  @@index([assignedToUserId, status])
  @@index([preferredCountryId, preferredCourseId])
  @@index([preferredSubjectId])
  @@index([preferredCourseLevelId])
  @@index([preferredIntakeId])
  @@map("leads")
}

model CounsellingBooking {
  id                 String    @id @default(uuid()) @db.Char(36)
  leadId             String    @map("lead_id") @db.Char(36)
  bookingDate        DateTime  @map("booking_date") @db.Date
  slotStartAt        DateTime  @map("slot_start_at") @db.DateTime(3)
  slotEndAt          DateTime? @map("slot_end_at") @db.DateTime(3)
  timezone           String    @default("Asia/Kolkata") @db.VarChar(100)
  bookingMode        String    @default("PHONE") @map("booking_mode") @db.VarChar(30)
  bookingStatus      String    @default("PENDING") @map("booking_status") @db.VarChar(30)
  assignedToUserId   String?   @map("assigned_to_user_id") @db.Char(36)
  meetingUrl         String?   @map("meeting_url") @db.VarChar(2048)
  meetingProvider    String?   @map("meeting_provider") @db.VarChar(50)
  meetingId          String?   @map("meeting_id") @db.VarChar(255)
  customerNotes      String?   @map("customer_notes") @db.Text
  internalNotes      String?   @map("internal_notes") @db.Text
  confirmedAt        DateTime? @map("confirmed_at") @db.DateTime(3)
  completedAt        DateTime? @map("completed_at") @db.DateTime(3)
  cancelledAt        DateTime? @map("cancelled_at") @db.DateTime(3)
  cancellationReason String?   @map("cancellation_reason") @db.VarChar(500)
  createdAt          DateTime  @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt          DateTime  @updatedAt @map("updated_at") @db.DateTime(3)

  lead       Lead  @relation(fields: [leadId], references: [id], onDelete: Cascade)
  assignedTo User? @relation("BookingAssignedTo", fields: [assignedToUserId], references: [id], onDelete: SetNull)

  @@index([leadId])
  @@index([bookingDate, bookingStatus])
  @@index([assignedToUserId, slotStartAt])
  @@map("counselling_bookings")
}

model LeadNote {
  id        String    @id @default(uuid()) @db.Char(36)
  leadId    String    @map("lead_id") @db.Char(36)
  userId    String    @map("user_id") @db.Char(36)
  noteType  String    @default("GENERAL") @map("note_type") @db.VarChar(30)
  note      String    @db.LongText
  isPinned  Boolean   @default(false) @map("is_pinned")
  createdAt DateTime  @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt DateTime  @updatedAt @map("updated_at") @db.DateTime(3)
  deletedAt DateTime? @map("deleted_at") @db.DateTime(3)

  lead Lead @relation(fields: [leadId], references: [id], onDelete: Cascade)
  user User @relation(fields: [userId], references: [id], onDelete: Restrict)

  @@index([leadId, isPinned, createdAt])
  @@index([userId])
  @@map("lead_notes")
}

model LeadStatusHistory {
  id              String   @id @default(uuid()) @db.Char(36)
  leadId          String   @map("lead_id") @db.Char(36)
  oldStatus       String?  @map("old_status") @db.VarChar(30)
  newStatus       String   @map("new_status") @db.VarChar(30)
  changedByUserId String?  @map("changed_by_user_id") @db.Char(36)
  reason          String?  @db.VarChar(500)
  createdAt       DateTime @default(now()) @map("created_at") @db.DateTime(3)

  lead      Lead  @relation(fields: [leadId], references: [id], onDelete: Cascade)
  changedBy User? @relation("LeadStatusChangedBy", fields: [changedByUserId], references: [id], onDelete: SetNull)

  @@index([leadId, createdAt])
  @@index([changedByUserId])
  @@map("lead_status_history")
}

model EmailTemplate {
  id              String   @id @default(uuid()) @db.Char(36)
  templateKey     String   @unique @map("template_key") @db.VarChar(150)
  name            String   @db.VarChar(255)
  subjectTemplate String   @map("subject_template") @db.VarChar(500)
  bodyHtml        String   @map("body_html") @db.LongText
  bodyText        String?  @map("body_text") @db.LongText
  variablesJson   Json?    @map("variables_json") @db.Json
  senderName      String?  @map("sender_name") @db.VarChar(255)
  replyToEmail    String?  @map("reply_to_email") @db.VarChar(255)
  status          String   @default("ACTIVE") @db.VarChar(30)
  createdByUserId String?  @map("created_by_user_id") @db.Char(36)
  updatedByUserId String?  @map("updated_by_user_id") @db.Char(36)
  createdAt       DateTime @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt       DateTime @updatedAt @map("updated_at") @db.DateTime(3)

  createdBy User?      @relation("EmailTemplateCreatedBy", fields: [createdByUserId], references: [id], onDelete: SetNull)
  updatedBy User?      @relation("EmailTemplateUpdatedBy", fields: [updatedByUserId], references: [id], onDelete: SetNull)
  emailLogs EmailLog[]

  @@index([createdByUserId])
  @@index([updatedByUserId])
  @@map("email_templates")
}

model EmailLog {
  id                String    @id @default(uuid()) @db.Char(36)
  templateId        String?   @map("template_id") @db.Char(36)
  relatedEntityType String?   @map("related_entity_type") @db.VarChar(50)
  relatedEntityId   String?   @map("related_entity_id") @db.Char(36)
  recipientEmail    String    @map("recipient_email") @db.VarChar(255)
  recipientName     String?   @map("recipient_name") @db.VarChar(255)
  subject           String    @db.VarChar(500)
  provider          String?   @db.VarChar(50)
  providerMessageId String?   @map("provider_message_id") @db.VarChar(255)
  status            String    @default("QUEUED") @db.VarChar(30)
  errorMessage      String?   @map("error_message") @db.Text
  queuedAt          DateTime? @map("queued_at") @db.DateTime(3)
  sentAt            DateTime? @map("sent_at") @db.DateTime(3)
  deliveredAt       DateTime? @map("delivered_at") @db.DateTime(3)
  failedAt          DateTime? @map("failed_at") @db.DateTime(3)
  createdAt         DateTime  @default(now()) @map("created_at") @db.DateTime(3)

  template EmailTemplate? @relation(fields: [templateId], references: [id], onDelete: SetNull)

  @@index([templateId])
  @@index([status, createdAt])
  @@index([relatedEntityType, relatedEntityId])
  @@map("email_logs")
}

model SearchLog {
  id                 String   @id @default(uuid()) @db.Char(36)
  searchType         String   @map("search_type") @db.VarChar(30)
  queryText          String?  @map("query_text") @db.VarChar(500)
  normalizedQuery    String?  @map("normalized_query") @db.VarChar(500)
  filtersJson        Json?    @map("filters_json") @db.Json
  sorting            String?  @db.VarChar(100)
  resultCount        Int      @default(0) @map("result_count")
  selectedEntityType String?  @map("selected_entity_type") @db.VarChar(50)
  selectedEntityId   String?  @map("selected_entity_id") @db.Char(36)
  sessionId          String?  @map("session_id") @db.VarChar(255)
  ipHash             String?  @map("ip_hash") @db.VarChar(255)
  userAgent          String?  @map("user_agent") @db.VarChar(1000)
  sourcePageUrl      String?  @map("source_page_url") @db.VarChar(2048)
  createdAt          DateTime @default(now()) @map("created_at") @db.DateTime(3)

  @@index([searchType, createdAt])
  @@index([normalizedQuery])
  @@map("search_logs")
}

model University {
  id               String    @id @default(uuid()) @db.Char(36)
  countryId        String    @map("country_id") @db.Char(36)
  name             String    @db.VarChar(255)
  slug             String    @unique @db.VarChar(255)
  institutionType  String?   @map("institution_type") @db.VarChar(80)
  qsRanking        Int?      @map("qs_ranking")
  shortDescription String?   @map("short_description") @db.VarChar(1000)
  overview         String?   @db.LongText
  featuredMediaId  String?   @map("featured_media_id") @db.Char(36)
  sourceReference  String?   @map("source_reference") @db.VarChar(2048)
  verifiedAt       DateTime? @map("verified_at") @db.DateTime(3)
  status           String    @default("DRAFT") @db.VarChar(30)
  isFeatured       Boolean   @default(false) @map("is_featured")
  featuredPriority Int       @default(0) @map("featured_priority")
  featuredFrom     DateTime? @map("featured_from") @db.DateTime(3)
  featuredUntil    DateTime? @map("featured_until") @db.DateTime(3)
  publishStartsAt  DateTime? @map("publish_starts_at") @db.DateTime(3)
  publishEndsAt    DateTime? @map("publish_ends_at") @db.DateTime(3)
  displayOrder     Int       @default(0) @map("display_order")
  publishedAt      DateTime? @map("published_at") @db.DateTime(3)
  createdAt        DateTime  @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt        DateTime  @updatedAt @map("updated_at") @db.DateTime(3)
  deletedAt        DateTime? @map("deleted_at") @db.DateTime(3)

  country             Country                    @relation(fields: [countryId], references: [id], onDelete: Restrict)
  campuses            UniversityCampus[]
  accreditations      UniversityAccreditation[]
  offerings           UniversityCourseOffering[]
  scholarships        ScholarshipUniversity[]
  successStories      SuccessStory[]
  testimonials        Testimonial[]
  claimRequests       UniversityClaimRequest[]
  studentSavedBy      StudentSavedUniversity[]
  studentApplications StudentApplication[]
  popularInCountries  CountryPopularUniversity[]

  @@unique([countryId, name])
  @@index([countryId, status, displayOrder])
  @@index([status, institutionType, isFeatured])
  @@index([countryId, qsRanking])
  @@map("universities")
}

/// Editorial selection, deliberately distinct from a Country's automatically
/// derived top-ranked universities. `displayOrder` keeps curated frontend
/// order stable without copying University content into Country rows.
model CountryPopularUniversity {
  countryId    String   @map("country_id") @db.Char(36)
  universityId String   @map("university_id") @db.Char(36)
  displayOrder Int      @default(0) @map("display_order")
  createdAt    DateTime @default(now()) @map("created_at") @db.DateTime(3)

  country    Country    @relation(fields: [countryId], references: [id], onDelete: Cascade)
  university University @relation(fields: [universityId], references: [id], onDelete: Cascade)

  @@id([countryId, universityId])
  @@index([universityId])
  @@map("country_popular_universities")
}

/// Editorial selection of existing generic Courses that are available through
/// the Country's university/course relationships.
model CountryPopularCourse {
  countryId    String   @map("country_id") @db.Char(36)
  courseId     String   @map("course_id") @db.Char(36)
  displayOrder Int      @default(0) @map("display_order")
  createdAt    DateTime @default(now()) @map("created_at") @db.DateTime(3)

  country Country @relation(fields: [countryId], references: [id], onDelete: Cascade)
  course  Course  @relation(fields: [courseId], references: [id], onDelete: Cascade)

  @@id([countryId, courseId])
  @@index([courseId])
  @@map("country_popular_courses")
}

model UniversityClaimRequest {
  id               String    @id @default(uuid()) @db.Char(36)
  universityId     String    @map("university_id") @db.Char(36)
  claimNumber      String    @unique @map("claim_number") @db.VarChar(50)
  claimantName     String    @map("claimant_name") @db.VarChar(255)
  workEmail        String    @map("work_email") @db.VarChar(255)
  jobTitle         String?   @map("job_title") @db.VarChar(255)
  organization     String?   @db.VarChar(255)
  phoneNumber      String?   @map("phone_number") @db.VarChar(50)
  officialWebsite  String?   @map("official_website") @db.VarChar(2048)
  message          String    @db.Text
  status           String    @default("SUBMITTED") @db.VarChar(30)
  reviewedByUserId String?   @map("reviewed_by_user_id") @db.Char(36)
  reviewedAt       DateTime? @map("reviewed_at") @db.DateTime(3)
  createdAt        DateTime  @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt        DateTime  @updatedAt @map("updated_at") @db.DateTime(3)
  deletedAt        DateTime? @map("deleted_at") @db.DateTime(3)

  university    University                     @relation(fields: [universityId], references: [id], onDelete: Cascade)
  reviewedBy    User?                          @relation("UniversityClaimReviewedBy", fields: [reviewedByUserId], references: [id], onDelete: SetNull)
  notes         UniversityClaimNote[]
  statusHistory UniversityClaimStatusHistory[]

  @@index([universityId, status])
  @@index([workEmail, status])
  @@map("university_claim_requests")
}

model UniversityClaimNote {
  id        String   @id @default(uuid()) @db.Char(36)
  claimId   String   @map("claim_id") @db.Char(36)
  userId    String   @map("user_id") @db.Char(36)
  note      String   @db.LongText
  createdAt DateTime @default(now()) @map("created_at") @db.DateTime(3)

  claim UniversityClaimRequest @relation(fields: [claimId], references: [id], onDelete: Cascade)
  user  User                   @relation(fields: [userId], references: [id], onDelete: Restrict)

  @@index([claimId, createdAt])
  @@index([userId])
  @@map("university_claim_notes")
}

model UniversityClaimStatusHistory {
  id              String   @id @default(uuid()) @db.Char(36)
  claimId         String   @map("claim_id") @db.Char(36)
  oldStatus       String?  @map("old_status") @db.VarChar(30)
  newStatus       String   @map("new_status") @db.VarChar(30)
  changedByUserId String?  @map("changed_by_user_id") @db.Char(36)
  reason          String?  @db.VarChar(500)
  createdAt       DateTime @default(now()) @map("created_at") @db.DateTime(3)

  claim     UniversityClaimRequest @relation(fields: [claimId], references: [id], onDelete: Cascade)
  changedBy User?                  @relation("UniversityClaimStatusChangedBy", fields: [changedByUserId], references: [id], onDelete: SetNull)

  @@index([claimId, createdAt])
  @@index([changedByUserId])
  @@map("university_claim_status_history")
}

model UniversityCampus {
  id           String    @id @default(uuid()) @db.Char(36)
  universityId String    @map("university_id") @db.Char(36)
  name         String    @db.VarChar(255)
  slug         String    @db.VarChar(255)
  city         String?   @db.VarChar(150)
  state        String?   @db.VarChar(150)
  cityId       String?   @map("city_id") @db.Char(36)
  stateId      String?   @map("state_id") @db.Char(36)
  address      String?   @db.Text
  overview     String?   @db.Text
  status       String    @default("ACTIVE") @db.VarChar(30)
  displayOrder Int       @default(0) @map("display_order")
  createdAt    DateTime  @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt    DateTime  @updatedAt @map("updated_at") @db.DateTime(3)
  deletedAt    DateTime? @map("deleted_at") @db.DateTime(3)

  university University                 @relation(fields: [universityId], references: [id], onDelete: Cascade)
  offerings  UniversityCourseOffering[]
  cityRef    City?                      @relation("CampusCity", fields: [cityId], references: [id], onDelete: SetNull)
  stateRef   State?                     @relation("CampusState", fields: [stateId], references: [id], onDelete: SetNull)

  @@unique([universityId, slug])
  @@index([universityId, status, displayOrder])
  @@index([cityId])
  @@index([stateId])
  @@map("university_campuses")
}

model UniversityAccreditation {
  id           String    @id @default(uuid()) @db.Char(36)
  universityId String    @map("university_id") @db.Char(36)
  name         String    @db.VarChar(255)
  accreditor   String?   @db.VarChar(255)
  referenceUrl String?   @map("reference_url") @db.VarChar(2048)
  verifiedAt   DateTime? @map("verified_at") @db.DateTime(3)
  status       String    @default("ACTIVE") @db.VarChar(30)
  displayOrder Int       @default(0) @map("display_order")
  createdAt    DateTime  @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt    DateTime  @updatedAt @map("updated_at") @db.DateTime(3)
  deletedAt    DateTime? @map("deleted_at") @db.DateTime(3)

  university University @relation(fields: [universityId], references: [id], onDelete: Cascade)

  @@index([universityId, status, displayOrder])
  @@map("university_accreditations")
}

model UniversityCourseOffering {
  id               String    @id @default(uuid()) @db.Char(36)
  universityId     String    @map("university_id") @db.Char(36)
  genericCourseId  String    @map("generic_course_id") @db.Char(36)
  campusId         String?   @map("campus_id") @db.Char(36)
  name             String    @db.VarChar(255)
  slug             String    @unique @db.VarChar(255)
  courseCode       String?   @map("course_code") @db.VarChar(100)
  shortDescription String?   @map("short_description") @db.VarChar(1000)
  overview         String?   @db.LongText
  courseLevelId    String?   @map("course_level_id") @db.Char(36)
  studyMode        String?   @map("study_mode") @db.VarChar(50)
  durationMin      Decimal?  @map("duration_min") @db.Decimal(6, 2)
  durationMax      Decimal?  @map("duration_max") @db.Decimal(6, 2)
  durationUnit     String?   @map("duration_unit") @db.VarChar(30)
  tuitionMin       Decimal?  @map("tuition_min") @db.Decimal(12, 2)
  tuitionMax       Decimal?  @map("tuition_max") @db.Decimal(12, 2)
  currencyCode     String?   @map("currency_code") @db.Char(3)
  tuitionPeriod    String?   @map("tuition_period") @db.VarChar(30)
  applicationUrl   String?   @map("application_url") @db.VarChar(2048)
  sourceReference  String?   @map("source_reference") @db.VarChar(2048)
  verifiedAt       DateTime? @map("verified_at") @db.DateTime(3)
  featuredMediaId  String?   @map("featured_media_id") @db.Char(36)
  status           String    @default("DRAFT") @db.VarChar(30)
  isFeatured       Boolean   @default(false) @map("is_featured")
  featuredPriority Int       @default(0) @map("featured_priority")
  featuredFrom     DateTime? @map("featured_from") @db.DateTime(3)
  featuredUntil    DateTime? @map("featured_until") @db.DateTime(3)
  publishStartsAt  DateTime? @map("publish_starts_at") @db.DateTime(3)
  publishEndsAt    DateTime? @map("publish_ends_at") @db.DateTime(3)
  displayOrder     Int       @default(0) @map("display_order")
  publishedAt      DateTime? @map("published_at") @db.DateTime(3)
  createdAt        DateTime  @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt        DateTime  @updatedAt @map("updated_at") @db.DateTime(3)
  deletedAt        DateTime? @map("deleted_at") @db.DateTime(3)

  university          University                            @relation(fields: [universityId], references: [id], onDelete: Cascade)
  genericCourse       Course                                @relation(fields: [genericCourseId], references: [id], onDelete: Restrict)
  campus              UniversityCampus?                     @relation(fields: [campusId], references: [id], onDelete: SetNull)
  courseLevel         CourseLevel?                          @relation(fields: [courseLevelId], references: [id], onDelete: SetNull)
  intakes             UniversityCourseIntake[]
  requirements        UniversityCourseRequirement[]
  scholarships        ScholarshipUniversityCourseOffering[]
  successStories      SuccessStory[]
  testimonials        Testimonial[]
  studentSavedBy      StudentSavedOffering[]
  studentApplications StudentApplication[]

  @@unique([universityId, name])
  @@index([universityId, status, displayOrder])
  @@index([genericCourseId, status])
  @@index([campusId])
  @@index([courseLevelId])
  @@map("university_course_offerings")
}

model UniversityCourseIntake {
  id         String    @id @default(uuid()) @db.Char(36)
  offeringId String    @map("offering_id") @db.Char(36)
  intakeId   String    @map("intake_id") @db.Char(36)
  deadline   DateTime? @db.Date
  notes      String?   @db.VarChar(500)
  status     String    @default("ACTIVE") @db.VarChar(30)
  createdAt  DateTime  @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt  DateTime  @updatedAt @map("updated_at") @db.DateTime(3)

  offering UniversityCourseOffering @relation(fields: [offeringId], references: [id], onDelete: Cascade)
  intake   Intake                   @relation(fields: [intakeId], references: [id], onDelete: Restrict)

  @@unique([offeringId, intakeId])
  @@index([intakeId, status])
  @@map("university_course_intakes")
}

model UniversityCourseRequirement {
  id           String    @id @default(uuid()) @db.Char(36)
  offeringId   String    @map("offering_id") @db.Char(36)
  category     String    @db.VarChar(50)
  title        String    @db.VarChar(255)
  description  String?   @db.Text
  minimumScore Decimal?  @map("minimum_score") @db.Decimal(7, 2)
  status       String    @default("ACTIVE") @db.VarChar(30)
  displayOrder Int       @default(0) @map("display_order")
  createdAt    DateTime  @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt    DateTime  @updatedAt @map("updated_at") @db.DateTime(3)
  deletedAt    DateTime? @map("deleted_at") @db.DateTime(3)

  offering UniversityCourseOffering @relation(fields: [offeringId], references: [id], onDelete: Cascade)

  @@index([offeringId, status, displayOrder])
  @@map("university_course_requirements")
}

model ScholarshipProvider {
  id              String    @id @default(uuid()) @db.Char(36)
  name            String    @unique @db.VarChar(255)
  slug            String    @unique @db.VarChar(255)
  websiteUrl      String?   @map("website_url") @db.VarChar(2048)
  sourceReference String?   @map("source_reference") @db.VarChar(2048)
  status          String    @default("ACTIVE") @db.VarChar(30)
  createdAt       DateTime  @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt       DateTime  @updatedAt @map("updated_at") @db.DateTime(3)
  deletedAt       DateTime? @map("deleted_at") @db.DateTime(3)

  scholarships Scholarship[]

  @@index([status])
  @@map("scholarship_providers")
}

model Scholarship {
  id               String    @id @default(uuid()) @db.Char(36)
  providerId       String?   @map("provider_id") @db.Char(36)
  title            String    @db.VarChar(255)
  slug             String    @unique @db.VarChar(255)
  summary          String?   @db.VarChar(1000)
  description      String?   @db.LongText
  benefitType      String?   @map("benefit_type") @db.VarChar(80)
  amount           Decimal?  @db.Decimal(12, 2)
  currencyCode     String?   @map("currency_code") @db.Char(3)
  eligibility      String?   @db.LongText
  deadline         DateTime? @db.Date
  applicationUrl   String?   @map("application_url") @db.VarChar(2048)
  sourceReference  String?   @map("source_reference") @db.VarChar(2048)
  verifiedAt       DateTime? @map("verified_at") @db.DateTime(3)
  featuredMediaId  String?   @map("featured_media_id") @db.Char(36)
  status           String    @default("DRAFT") @db.VarChar(30)
  isFeatured       Boolean   @default(false) @map("is_featured")
  featuredPriority Int       @default(0) @map("featured_priority")
  featuredFrom     DateTime? @map("featured_from") @db.DateTime(3)
  featuredUntil    DateTime? @map("featured_until") @db.DateTime(3)
  publishStartsAt  DateTime? @map("publish_starts_at") @db.DateTime(3)
  publishEndsAt    DateTime? @map("publish_ends_at") @db.DateTime(3)
  displayOrder     Int       @default(0) @map("display_order")
  publishedAt      DateTime? @map("published_at") @db.DateTime(3)
  createdAt        DateTime  @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt        DateTime  @updatedAt @map("updated_at") @db.DateTime(3)
  deletedAt        DateTime? @map("deleted_at") @db.DateTime(3)

  provider            ScholarshipProvider?                  @relation(fields: [providerId], references: [id], onDelete: SetNull)
  countries           ScholarshipCountry[]
  universities        ScholarshipUniversity[]
  offerings           ScholarshipUniversityCourseOffering[]
  studentSavedBy      StudentSavedScholarship[]
  studentApplications StudentScholarshipApplication[]

  @@index([providerId, status, displayOrder])
  @@index([status, deadline])
  @@map("scholarships")
}

model ScholarshipCountry {
  scholarshipId String      @map("scholarship_id") @db.Char(36)
  countryId     String      @map("country_id") @db.Char(36)
  scholarship   Scholarship @relation(fields: [scholarshipId], references: [id], onDelete: Cascade)
  country       Country     @relation(fields: [countryId], references: [id], onDelete: Cascade)

  @@id([scholarshipId, countryId])
  @@index([countryId])
  @@map("scholarship_countries")
}

model ScholarshipUniversity {
  scholarshipId String      @map("scholarship_id") @db.Char(36)
  universityId  String      @map("university_id") @db.Char(36)
  scholarship   Scholarship @relation(fields: [scholarshipId], references: [id], onDelete: Cascade)
  university    University  @relation(fields: [universityId], references: [id], onDelete: Cascade)

  @@id([scholarshipId, universityId])
  @@index([universityId])
  @@map("scholarship_universities")
}

model ScholarshipUniversityCourseOffering {
  scholarshipId String                   @map("scholarship_id") @db.Char(36)
  offeringId    String                   @map("offering_id") @db.Char(36)
  scholarship   Scholarship              @relation(fields: [scholarshipId], references: [id], onDelete: Cascade)
  offering      UniversityCourseOffering @relation(fields: [offeringId], references: [id], onDelete: Cascade)

  @@id([scholarshipId, offeringId])
  @@index([offeringId])
  @@map("scholarship_university_course_offerings")
}

model Consultant {
  id                 String    @id @default(uuid()) @db.Char(36)
  name               String    @db.VarChar(255)
  slug               String    @unique @db.VarChar(255)
  shortDescription   String?   @map("short_description") @db.VarChar(1000)
  description        String?   @db.LongText
  email              String?   @db.VarChar(255)
  phone              String?   @db.VarChar(50)
  websiteUrl         String?   @map("website_url") @db.VarChar(2048)
  verificationStatus String    @default("UNVERIFIED") @map("verification_status") @db.VarChar(30)
  sourceReference    String?   @map("source_reference") @db.VarChar(2048)
  verifiedAt         DateTime? @map("verified_at") @db.DateTime(3)
  featuredMediaId    String?   @map("featured_media_id") @db.Char(36)
  status             String    @default("DRAFT") @db.VarChar(30)
  isFeatured         Boolean   @default(false) @map("is_featured")
  featuredPriority   Int       @default(0) @map("featured_priority")
  featuredFrom       DateTime? @map("featured_from") @db.DateTime(3)
  featuredUntil      DateTime? @map("featured_until") @db.DateTime(3)
  publishStartsAt    DateTime? @map("publish_starts_at") @db.DateTime(3)
  publishEndsAt      DateTime? @map("publish_ends_at") @db.DateTime(3)
  displayOrder       Int       @default(0) @map("display_order")
  publishedAt        DateTime? @map("published_at") @db.DateTime(3)
  createdAt          DateTime  @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt          DateTime  @updatedAt @map("updated_at") @db.DateTime(3)
  deletedAt          DateTime? @map("deleted_at") @db.DateTime(3)

  locations          ConsultantLocationMap[]
  services           ConsultantService[]
  countries          ConsultantCountry[]
  languages          ConsultantLanguage[]
  studentAssignments StudentConsultantAssignment[]

  @@index([status, verificationStatus, displayOrder])
  @@map("consultants")
}

model ConsultantLocation {
  id        String    @id @default(uuid()) @db.Char(36)
  countryId String?   @map("country_id") @db.Char(36)
  name      String    @db.VarChar(255)
  slug      String    @unique @db.VarChar(255)
  city      String    @db.VarChar(150)
  state     String?   @db.VarChar(150)
  cityId    String?   @map("city_id") @db.Char(36)
  stateId   String?   @map("state_id") @db.Char(36)
  overview  String?   @db.Text
  status    String    @default("ACTIVE") @db.VarChar(30)
  createdAt DateTime  @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt DateTime  @updatedAt @map("updated_at") @db.DateTime(3)
  deletedAt DateTime? @map("deleted_at") @db.DateTime(3)

  country     Country?                @relation(fields: [countryId], references: [id], onDelete: SetNull)
  consultants ConsultantLocationMap[]
  cityRef     City?                   @relation("ConsultantLocationCity", fields: [cityId], references: [id], onDelete: SetNull)
  stateRef    State?                  @relation("ConsultantLocationState", fields: [stateId], references: [id], onDelete: SetNull)

  @@index([countryId, status])
  @@index([cityId])
  @@index([stateId])
  @@map("consultant_locations")
}

model ConsultantLocationMap {
  consultantId String             @map("consultant_id") @db.Char(36)
  locationId   String             @map("location_id") @db.Char(36)
  address      String?            @db.Text
  consultant   Consultant         @relation(fields: [consultantId], references: [id], onDelete: Cascade)
  location     ConsultantLocation @relation(fields: [locationId], references: [id], onDelete: Cascade)

  @@id([consultantId, locationId])
  @@index([locationId])
  @@map("consultant_location_map")
}

model ConsultantService {
  id           String     @id @default(uuid()) @db.Char(36)
  consultantId String     @map("consultant_id") @db.Char(36)
  name         String     @db.VarChar(150)
  slug         String     @db.VarChar(150)
  displayOrder Int        @default(0) @map("display_order")
  consultant   Consultant @relation(fields: [consultantId], references: [id], onDelete: Cascade)

  @@unique([consultantId, slug])
  @@index([slug])
  @@map("consultant_services")
}

model ConsultantCountry {
  consultantId String     @map("consultant_id") @db.Char(36)
  countryId    String     @map("country_id") @db.Char(36)
  consultant   Consultant @relation(fields: [consultantId], references: [id], onDelete: Cascade)
  country      Country    @relation(fields: [countryId], references: [id], onDelete: Cascade)

  @@id([consultantId, countryId])
  @@index([countryId])
  @@map("consultant_countries")
}

model ConsultantLanguage {
  id           String     @id @default(uuid()) @db.Char(36)
  consultantId String     @map("consultant_id") @db.Char(36)
  name         String     @db.VarChar(100)
  code         String?    @db.VarChar(20)
  consultant   Consultant @relation(fields: [consultantId], references: [id], onDelete: Cascade)

  @@unique([consultantId, name])
  @@index([name])
  @@map("consultant_languages")
}

model ContactInquiry {
  id                String    @id @default(uuid()) @db.Char(36)
  inquiryNumber     String    @unique @map("inquiry_number") @db.VarChar(50)
  fullName          String    @map("full_name") @db.VarChar(255)
  email             String    @db.VarChar(255)
  phoneNumber       String?   @map("phone_number") @db.VarChar(50)
  subject           String?   @db.VarChar(255)
  message           String    @db.Text
  privacyConsent    Boolean   @default(false) @map("privacy_consent")
  status            String    @default("NEW") @db.VarChar(30)
  convertedLeadId   String?   @unique @map("converted_lead_id") @db.Char(36)
  convertedByUserId String?   @map("converted_by_user_id") @db.Char(36)
  convertedAt       DateTime? @map("converted_at") @db.DateTime(3)
  createdAt         DateTime  @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt         DateTime  @updatedAt @map("updated_at") @db.DateTime(3)
  deletedAt         DateTime? @map("deleted_at") @db.DateTime(3)

  convertedLead Lead? @relation("ContactInquiryConvertedLead", fields: [convertedLeadId], references: [id], onDelete: SetNull)
  convertedBy   User? @relation("ContactInquiryConvertedBy", fields: [convertedByUserId], references: [id], onDelete: SetNull)

  @@index([status, createdAt])
  @@index([email, createdAt])
  @@map("contact_inquiries")
}

model SuccessStory {
  id              String    @id @default(uuid()) @db.Char(36)
  countryId       String?   @map("country_id") @db.Char(36)
  universityId    String?   @map("university_id") @db.Char(36)
  offeringId      String?   @map("offering_id") @db.Char(36)
  title           String    @db.VarChar(255)
  slug            String    @unique @db.VarChar(255)
  journey         String    @db.LongText
  attribution     String?   @db.VarChar(255)
  attributionNote String?   @map("attribution_note") @db.VarChar(500)
  featuredMediaId String?   @map("featured_media_id") @db.Char(36)
  status          String    @default("DRAFT") @db.VarChar(30)
  publishStartsAt DateTime? @map("publish_starts_at") @db.DateTime(3)
  publishEndsAt   DateTime? @map("publish_ends_at") @db.DateTime(3)
  displayOrder    Int       @default(0) @map("display_order")
  publishedAt     DateTime? @map("published_at") @db.DateTime(3)
  createdAt       DateTime  @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt       DateTime  @updatedAt @map("updated_at") @db.DateTime(3)
  deletedAt       DateTime? @map("deleted_at") @db.DateTime(3)

  country    Country?                  @relation(fields: [countryId], references: [id], onDelete: SetNull)
  university University?               @relation(fields: [universityId], references: [id], onDelete: SetNull)
  offering   UniversityCourseOffering? @relation(fields: [offeringId], references: [id], onDelete: SetNull)

  @@index([status, displayOrder])
  @@map("success_stories")
}

model Testimonial {
  id              String                    @id @default(uuid()) @db.Char(36)
  universityId    String?                   @map("university_id") @db.Char(36)
  offeringId      String?                   @map("offering_id") @db.Char(36)
  quote           String                    @db.Text
  attribution     String?                   @db.VarChar(255)
  attributionNote String?                   @map("attribution_note") @db.VarChar(500)
  imageMediaId    String?                   @map("image_media_id") @db.Char(36)
  status          String                    @default("DRAFT") @db.VarChar(30)
  publishStartsAt DateTime?                 @map("publish_starts_at") @db.DateTime(3)
  publishEndsAt   DateTime?                 @map("publish_ends_at") @db.DateTime(3)
  displayOrder    Int                       @default(0) @map("display_order")
  publishedAt     DateTime?                 @map("published_at") @db.DateTime(3)
  createdAt       DateTime                  @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt       DateTime                  @updatedAt @map("updated_at") @db.DateTime(3)
  deletedAt       DateTime?                 @map("deleted_at") @db.DateTime(3)
  university      University?               @relation(fields: [universityId], references: [id], onDelete: SetNull)
  offering        UniversityCourseOffering? @relation(fields: [offeringId], references: [id], onDelete: SetNull)

  @@index([status, displayOrder])
  @@map("testimonials")
}

model Job {
  id               String    @id @default(uuid()) @db.Char(36)
  title            String    @db.VarChar(255)
  slug             String    @unique @db.VarChar(255)
  department       String?   @db.VarChar(150)
  employmentType   String?   @map("employment_type") @db.VarChar(80)
  location         String?   @db.VarChar(255)
  cityId           String?   @map("city_id") @db.Char(36)
  stateId          String?   @map("state_id") @db.Char(36)
  countryId        String?   @map("country_id") @db.Char(36)
  remoteStatus     String?   @map("remote_status") @db.VarChar(50)
  summary          String?   @db.VarChar(1000)
  description      String?   @db.LongText
  responsibilities String?   @db.LongText
  qualifications   String?   @db.LongText
  applicationUrl   String?   @map("application_url") @db.VarChar(2048)
  applicationEmail String?   @map("application_email") @db.VarChar(255)
  publishedDate    DateTime? @map("published_date") @db.Date
  expiryDate       DateTime? @map("expiry_date") @db.Date
  status           String    @default("DRAFT") @db.VarChar(30)
  isFeatured       Boolean   @default(false) @map("is_featured")
  featuredPriority Int       @default(0) @map("featured_priority")
  featuredFrom     DateTime? @map("featured_from") @db.DateTime(3)
  featuredUntil    DateTime? @map("featured_until") @db.DateTime(3)
  publishStartsAt  DateTime? @map("publish_starts_at") @db.DateTime(3)
  publishEndsAt    DateTime? @map("publish_ends_at") @db.DateTime(3)
  displayOrder     Int       @default(0) @map("display_order")
  publishedAt      DateTime? @map("published_at") @db.DateTime(3)
  createdAt        DateTime  @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt        DateTime  @updatedAt @map("updated_at") @db.DateTime(3)
  deletedAt        DateTime? @map("deleted_at") @db.DateTime(3)

  city    City?    @relation("JobCity", fields: [cityId], references: [id], onDelete: SetNull)
  state   State?   @relation("JobState", fields: [stateId], references: [id], onDelete: SetNull)
  country Country? @relation("JobCountry", fields: [countryId], references: [id], onDelete: SetNull)

  @@index([status, expiryDate, displayOrder])
  @@index([cityId])
  @@index([stateId])
  @@index([countryId])
  @@map("jobs")
}

model Event {
  id               String    @id @default(uuid()) @db.Char(36)
  title            String    @db.VarChar(255)
  slug             String    @unique @db.VarChar(255)
  summary          String?   @db.VarChar(1000)
  description      String?   @db.LongText
  startsAt         DateTime  @map("starts_at") @db.DateTime(3)
  endsAt           DateTime? @map("ends_at") @db.DateTime(3)
  timezone         String    @default("Asia/Kolkata") @db.VarChar(100)
  eventType        String    @default("OFFLINE") @map("event_type") @db.VarChar(30)
  venue            String?   @db.VarChar(500)
  cityId           String?   @map("city_id") @db.Char(36)
  stateId          String?   @map("state_id") @db.Char(36)
  countryId        String?   @map("country_id") @db.Char(36)
  onlineUrl        String?   @map("online_url") @db.VarChar(2048)
  speakersJson     Json?     @map("speakers_json") @db.Json
  registrationUrl  String?   @map("registration_url") @db.VarChar(2048)
  featuredMediaId  String?   @map("featured_media_id") @db.Char(36)
  status           String    @default("DRAFT") @db.VarChar(30)
  isFeatured       Boolean   @default(false) @map("is_featured")
  featuredPriority Int       @default(0) @map("featured_priority")
  featuredFrom     DateTime? @map("featured_from") @db.DateTime(3)
  featuredUntil    DateTime? @map("featured_until") @db.DateTime(3)
  publishStartsAt  DateTime? @map("publish_starts_at") @db.DateTime(3)
  publishEndsAt    DateTime? @map("publish_ends_at") @db.DateTime(3)
  displayOrder     Int       @default(0) @map("display_order")
  publishedAt      DateTime? @map("published_at") @db.DateTime(3)
  createdAt        DateTime  @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt        DateTime  @updatedAt @map("updated_at") @db.DateTime(3)
  deletedAt        DateTime? @map("deleted_at") @db.DateTime(3)

  city    City?    @relation("EventCity", fields: [cityId], references: [id], onDelete: SetNull)
  state   State?   @relation("EventState", fields: [stateId], references: [id], onDelete: SetNull)
  country Country? @relation("EventCountry", fields: [countryId], references: [id], onDelete: SetNull)

  @@index([status, startsAt, displayOrder])
  @@index([cityId])
  @@index([stateId])
  @@index([countryId])
  @@map("events")
}

/// Version history for Website Builder content.
///
/// One table for every versioned resource rather than a snapshot column per
/// model: the Admin list, compare and restore flows are identical across
/// Pages, Page Sections, Page Templates and the Global Header/Footer settings
/// groups, and a single table keeps them that way.
///
/// `snapshotJson` holds the full state of the resource at that point, so a
/// restore never has to reconstruct anything from diffs. `versionNumber` is
/// per-resource and monotonic: a restore appends a new highest version rather
/// than rewinding the counter, so later history is never lost.
model ContentVersion {
  id                  String   @id @default(uuid()) @db.Char(36)
  /// PAGE | PAGE_SECTION | PAGE_TEMPLATE | GLOBAL_HEADER | GLOBAL_FOOTER
  resourceType        String   @map("resource_type") @db.VarChar(40)
  /// Page/section/template id, or the settings group key for header/footer.
  resourceId          String   @map("resource_id") @db.VarChar(150)
  versionNumber       Int      @map("version_number")
  snapshotJson        Json     @map("snapshot_json") @db.Json
  /// Human-readable summary of what changed, shown in the Admin list.
  changeSummary       String   @map("change_summary") @db.VarChar(500)
  /// The editor action that produced this version (update, reorder,
  /// visibility, publish, restore, ...), for filtering and audit.
  sourceAction        String   @map("source_action") @db.VarChar(60)
  /// Set only on versions produced by a restore, naming the version restored.
  restoredFromVersion Int?     @map("restored_from_version")
  createdByUserId     String?  @map("created_by_user_id") @db.Char(36)
  createdAt           DateTime @default(now()) @map("created_at") @db.DateTime(3)

  createdBy User? @relation("ContentVersionCreatedBy", fields: [createdByUserId], references: [id], onDelete: SetNull)

  @@unique([resourceType, resourceId, versionNumber])
  @@index([resourceType, resourceId, createdAt])
  @@index([createdByUserId])
  @@map("content_versions")
}

// ===========================================================================
// Student domain (Phase 2-A)
//
// `User` stays identity and account: credentials, status, roles. Everything a
// student tells us about themselves lives here instead, so the account table
// does not grow a column per question we ever ask. Catalogue master data
// (Country, Subject, CourseLevel, Intake) is referenced, never copied.
// ===========================================================================

model StudentProfile {
  id     String @id @default(uuid()) @db.Char(36)
  userId String @unique @map("user_id") @db.Char(36)

  dateOfBirth          DateTime? @map("date_of_birth") @db.Date
  gender               String?   @db.VarChar(30)
  nationalityCountryId String?   @map("nationality_country_id") @db.Char(36)
  currentCountryId     String?   @map("current_country_id") @db.Char(36)
  currentCityText      String?   @map("current_city_text") @db.VarChar(150)
  address              String?   @db.VarChar(500)
  postalCode           String?   @map("postal_code") @db.VarChar(20)

  // Study preferences. Single-value preferences are foreign keys; the
  // destinations a student is open to are many, so they get a join table.
  preferredSubjectId     String?  @map("preferred_subject_id") @db.Char(36)
  preferredCourseLevelId String?  @map("preferred_course_level_id") @db.Char(36)
  preferredIntakeId      String?  @map("preferred_intake_id") @db.Char(36)
  budgetMin              Decimal? @map("budget_min") @db.Decimal(12, 2)
  budgetMax              Decimal? @map("budget_max") @db.Decimal(12, 2)
  budgetCurrency         String?  @map("budget_currency") @db.VarChar(3)
  referralCode           String?  @unique @map("referral_code") @db.VarChar(40)

  createdAt DateTime @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt DateTime @updatedAt @map("updated_at") @db.DateTime(3)

  user                    User                            @relation(fields: [userId], references: [id], onDelete: Cascade)
  nationalityCountry      Country?                        @relation("StudentNationality", fields: [nationalityCountryId], references: [id], onDelete: SetNull)
  currentCountry          Country?                        @relation("StudentCurrentCountry", fields: [currentCountryId], references: [id], onDelete: SetNull)
  preferredSubject        Subject?                        @relation("StudentPreferredSubject", fields: [preferredSubjectId], references: [id], onDelete: SetNull)
  preferredCourseLevel    CourseLevel?                    @relation("StudentPreferredCourseLevel", fields: [preferredCourseLevelId], references: [id], onDelete: SetNull)
  preferredIntake         Intake?                         @relation("StudentPreferredIntake", fields: [preferredIntakeId], references: [id], onDelete: SetNull)
  preferredCountries      StudentPreferredCountry[]
  academicRecords         StudentAcademicRecord[]
  workExperiences         StudentWorkExperience[]
  englishTests            StudentEnglishTest[]
  passport                StudentPassport?
  documents               StudentDocument[]
  savedUniversities       StudentSavedUniversity[]
  savedOfferings          StudentSavedOffering[]
  savedScholarships       StudentSavedScholarship[]
  applications            StudentApplication[]
  scholarshipApplications StudentScholarshipApplication[]
  consultantAssignments   StudentConsultantAssignment[]
  conversations           StudentConversation[]
  notifications           StudentNotification[]
  supportTickets          StudentSupportTicket[]
  referralsMade           StudentReferral[]               @relation("StudentReferralReferrer")
  referralReceived        StudentReferral?                @relation("StudentReferralReferred")

  @@index([nationalityCountryId])
  @@index([currentCountryId])
  @@map("student_profiles")
}

model StudentPreferredCountry {
  studentProfileId String @map("student_profile_id") @db.Char(36)
  countryId        String @map("country_id") @db.Char(36)
  displayOrder     Int    @default(0) @map("display_order")

  studentProfile StudentProfile @relation(fields: [studentProfileId], references: [id], onDelete: Cascade)
  country        Country        @relation("StudentPreferredCountry", fields: [countryId], references: [id], onDelete: Cascade)

  @@id([studentProfileId, countryId])
  @@index([countryId])
  @@map("student_preferred_countries")
}

/// One row per qualification. Deliberately text-based for institution and board
/// names: encoding every education system in the world is not the job here.
model StudentAcademicRecord {
  id               String @id @default(uuid()) @db.Char(36)
  studentProfileId String @map("student_profile_id") @db.Char(36)

  qualificationName  String    @map("qualification_name") @db.VarChar(200)
  qualificationLevel String?   @map("qualification_level") @db.VarChar(60)
  institutionName    String    @map("institution_name") @db.VarChar(200)
  boardOrUniversity  String?   @map("board_or_university") @db.VarChar(200)
  countryId          String?   @map("country_id") @db.Char(36)
  specialization     String?   @db.VarChar(200)
  startDate          DateTime? @map("start_date") @db.Date
  endDate            DateTime? @map("end_date") @db.Date
  currentlyStudying  Boolean   @default(false) @map("currently_studying")
  percentage         Decimal?  @db.Decimal(5, 2)
  gpa                Decimal?  @db.Decimal(5, 2)
  gpaScale           Decimal?  @map("gpa_scale") @db.Decimal(5, 2)
  notes              String?   @db.Text

  createdAt DateTime @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt DateTime @updatedAt @map("updated_at") @db.DateTime(3)

  studentProfile StudentProfile @relation(fields: [studentProfileId], references: [id], onDelete: Cascade)
  country        Country?       @relation("StudentAcademicCountry", fields: [countryId], references: [id], onDelete: SetNull)

  @@index([studentProfileId])
  @@index([countryId])
  @@map("student_academic_records")
}

model StudentWorkExperience {
  id               String @id @default(uuid()) @db.Char(36)
  studentProfileId String @map("student_profile_id") @db.Char(36)

  companyName      String    @map("company_name") @db.VarChar(200)
  jobTitle         String    @map("job_title") @db.VarChar(200)
  employmentType   String?   @map("employment_type") @db.VarChar(40)
  startDate        DateTime  @map("start_date") @db.Date
  endDate          DateTime? @map("end_date") @db.Date
  currentlyWorking Boolean   @default(false) @map("currently_working")
  description      String?   @db.Text

  createdAt DateTime @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt DateTime @updatedAt @map("updated_at") @db.DateTime(3)

  studentProfile StudentProfile @relation(fields: [studentProfileId], references: [id], onDelete: Cascade)

  @@index([studentProfileId])
  @@map("student_work_experiences")
}

/// Component scores are JSON because each exam scores different things: IELTS
/// has four bands, PTE and TOEFL do not match them one to one, and dedicated
/// columns for one exam make every other exam awkward.
model StudentEnglishTest {
  id               String @id @default(uuid()) @db.Char(36)
  studentProfileId String @map("student_profile_id") @db.Char(36)

  testType        String    @map("test_type") @db.VarChar(30)
  testDate        DateTime? @map("test_date") @db.Date
  overallScore    Decimal   @map("overall_score") @db.Decimal(5, 2)
  componentScores Json?     @map("component_scores")
  expiryDate      DateTime? @map("expiry_date") @db.Date
  resultMediaId   String?   @map("result_media_id") @db.Char(36)

  createdAt DateTime @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt DateTime @updatedAt @map("updated_at") @db.DateTime(3)

  studentProfile StudentProfile @relation(fields: [studentProfileId], references: [id], onDelete: Cascade)
  resultMedia    MediaAsset?    @relation("StudentEnglishTestResult", fields: [resultMediaId], references: [id], onDelete: SetNull)

  @@index([studentProfileId])
  @@map("student_english_tests")
}

/// Sensitive. Read and written only through the owner's own endpoint, never
/// included in list responses, and never written to logs or audit descriptions.
model StudentPassport {
  id               String @id @default(uuid()) @db.Char(36)
  studentProfileId String @unique @map("student_profile_id") @db.Char(36)

  passportNumber   String    @map("passport_number") @db.VarChar(60)
  issuingCountryId String?   @map("issuing_country_id") @db.Char(36)
  issueDate        DateTime? @map("issue_date") @db.Date
  expiryDate       DateTime? @map("expiry_date") @db.Date
  passportMediaId  String?   @map("passport_media_id") @db.Char(36)

  createdAt DateTime @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt DateTime @updatedAt @map("updated_at") @db.DateTime(3)

  studentProfile StudentProfile @relation(fields: [studentProfileId], references: [id], onDelete: Cascade)
  issuingCountry Country?       @relation("StudentPassportCountry", fields: [issuingCountryId], references: [id], onDelete: SetNull)
  passportMedia  MediaAsset?    @relation("StudentPassportScan", fields: [passportMediaId], references: [id], onDelete: SetNull)

  @@index([issuingCountryId])
  @@map("student_passports")
}

/// Ownership metadata for a file already stored as a MediaAsset. The bytes stay
/// in media storage; this row is what makes a file *this student's*.
model StudentDocument {
  id               String @id @default(uuid()) @db.Char(36)
  studentProfileId String @map("student_profile_id") @db.Char(36)
  mediaAssetId     String @map("media_asset_id") @db.Char(36)

  documentType String  @map("document_type") @db.VarChar(40)
  title        String  @db.VarChar(255)
  notes        String? @db.Text

  createdAt DateTime @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt DateTime @updatedAt @map("updated_at") @db.DateTime(3)

  studentProfile              StudentProfile               @relation(fields: [studentProfileId], references: [id], onDelete: Cascade)
  mediaAsset                  MediaAsset                   @relation("StudentDocumentMedia", fields: [mediaAssetId], references: [id], onDelete: Cascade)
  applicationLinks            StudentApplicationDocument[]
  scholarshipApplicationLinks StudentScholarshipDocument[]

  @@index([studentProfileId])
  @@index([studentProfileId, documentType])
  @@index([mediaAssetId])
  @@map("student_documents")
}

// Phase 2 lifecycle records deliberately keep catalogue relationships as
// foreign keys. Display snapshots are limited to the name a student saw at the
// moment they started an application, so historical pages remain readable
// after a catalogue item is withdrawn without copying catalogue payloads.
model StudentSavedUniversity {
  studentProfileId String         @map("student_profile_id") @db.Char(36)
  universityId     String         @map("university_id") @db.Char(36)
  createdAt        DateTime       @default(now()) @map("created_at") @db.DateTime(3)
  studentProfile   StudentProfile @relation(fields: [studentProfileId], references: [id], onDelete: Cascade)
  university       University     @relation(fields: [universityId], references: [id], onDelete: Cascade)

  @@id([studentProfileId, universityId])
  @@index([universityId])
  @@map("student_saved_universities")
}

model StudentSavedOffering {
  studentProfileId String                   @map("student_profile_id") @db.Char(36)
  offeringId       String                   @map("offering_id") @db.Char(36)
  createdAt        DateTime                 @default(now()) @map("created_at") @db.DateTime(3)
  studentProfile   StudentProfile           @relation(fields: [studentProfileId], references: [id], onDelete: Cascade)
  offering         UniversityCourseOffering @relation(fields: [offeringId], references: [id], onDelete: Cascade)

  @@id([studentProfileId, offeringId])
  @@index([offeringId])
  @@map("student_saved_offerings")
}

model StudentSavedScholarship {
  studentProfileId String         @map("student_profile_id") @db.Char(36)
  scholarshipId    String         @map("scholarship_id") @db.Char(36)
  createdAt        DateTime       @default(now()) @map("created_at") @db.DateTime(3)
  studentProfile   StudentProfile @relation(fields: [studentProfileId], references: [id], onDelete: Cascade)
  scholarship      Scholarship    @relation(fields: [scholarshipId], references: [id], onDelete: Cascade)

  @@id([studentProfileId, scholarshipId])
  @@index([scholarshipId])
  @@map("student_saved_scholarships")
}

model StudentApplication {
  id                     String                       @id @default(uuid()) @db.Char(36)
  studentProfileId       String                       @map("student_profile_id") @db.Char(36)
  universityId           String                       @map("university_id") @db.Char(36)
  offeringId             String?                      @map("offering_id") @db.Char(36)
  targetIntakeId         String?                      @map("target_intake_id") @db.Char(36)
  status                 String                       @default("APPLICATION_STARTED") @db.VarChar(40)
  universityNameSnapshot String                       @map("university_name_snapshot") @db.VarChar(255)
  offeringNameSnapshot   String?                      @map("offering_name_snapshot") @db.VarChar(255)
  submittedAt            DateTime?                    @map("submitted_at") @db.DateTime(3)
  decisionAt             DateTime?                    @map("decision_at") @db.DateTime(3)
  offerMediaId           String?                      @map("offer_media_id") @db.Char(36)
  offerDecision          String?                      @map("offer_decision") @db.VarChar(20)
  offerDecisionAt        DateTime?                    @map("offer_decision_at") @db.DateTime(3)
  createdAt              DateTime                     @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt              DateTime                     @updatedAt @map("updated_at") @db.DateTime(3)
  studentProfile         StudentProfile               @relation(fields: [studentProfileId], references: [id], onDelete: Cascade)
  university             University                   @relation(fields: [universityId], references: [id], onDelete: Restrict)
  offering               UniversityCourseOffering?    @relation(fields: [offeringId], references: [id], onDelete: SetNull)
  targetIntake           Intake?                      @relation(fields: [targetIntakeId], references: [id], onDelete: SetNull)
  offerMedia             MediaAsset?                  @relation("StudentApplicationOfferMedia", fields: [offerMediaId], references: [id], onDelete: SetNull)
  timeline               StudentApplicationTimeline[]
  documents              StudentApplicationDocument[]

  @@index([studentProfileId, status, updatedAt])
  @@index([universityId])
  @@index([offeringId])
  @@map("student_applications")
}

model StudentApplicationTimeline {
  id              String             @id @default(uuid()) @db.Char(36)
  applicationId   String             @map("application_id") @db.Char(36)
  status          String             @db.VarChar(40)
  message         String?            @db.VarChar(1000)
  actorType       String             @map("actor_type") @db.VarChar(20)
  changedByUserId String?            @map("changed_by_user_id") @db.Char(36)
  createdAt       DateTime           @default(now()) @map("created_at") @db.DateTime(3)
  application     StudentApplication @relation(fields: [applicationId], references: [id], onDelete: Cascade)
  changedBy       User?              @relation("ApplicationStatusChangedBy", fields: [changedByUserId], references: [id], onDelete: SetNull)

  @@index([applicationId, createdAt])
  @@map("student_application_timeline")
}

model StudentApplicationDocument {
  applicationId     String             @map("application_id") @db.Char(36)
  studentDocumentId String             @map("student_document_id") @db.Char(36)
  createdAt         DateTime           @default(now()) @map("created_at") @db.DateTime(3)
  application       StudentApplication @relation(fields: [applicationId], references: [id], onDelete: Cascade)
  studentDocument   StudentDocument    @relation(fields: [studentDocumentId], references: [id], onDelete: Cascade)

  @@id([applicationId, studentDocumentId])
  @@index([studentDocumentId])
  @@map("student_application_documents")
}

model StudentScholarshipApplication {
  id                       String                       @id @default(uuid()) @db.Char(36)
  studentProfileId         String                       @map("student_profile_id") @db.Char(36)
  scholarshipId            String                       @map("scholarship_id") @db.Char(36)
  status                   String                       @default("STARTED") @db.VarChar(30)
  scholarshipTitleSnapshot String                       @map("scholarship_title_snapshot") @db.VarChar(255)
  submittedAt              DateTime?                    @map("submitted_at") @db.DateTime(3)
  decisionAt               DateTime?                    @map("decision_at") @db.DateTime(3)
  createdAt                DateTime                     @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt                DateTime                     @updatedAt @map("updated_at") @db.DateTime(3)
  studentProfile           StudentProfile               @relation(fields: [studentProfileId], references: [id], onDelete: Cascade)
  scholarship              Scholarship                  @relation(fields: [scholarshipId], references: [id], onDelete: Restrict)
  timeline                 StudentScholarshipTimeline[]
  documents                StudentScholarshipDocument[]

  @@unique([studentProfileId, scholarshipId])
  @@index([studentProfileId, status, updatedAt])
  @@map("student_scholarship_applications")
}

model StudentScholarshipTimeline {
  id                       String                        @id @default(uuid()) @db.Char(36)
  scholarshipApplicationId String                        @map("scholarship_application_id") @db.Char(36)
  status                   String                        @db.VarChar(30)
  message                  String?                       @db.VarChar(1000)
  actorType                String                        @map("actor_type") @db.VarChar(20)
  changedByUserId          String?                       @map("changed_by_user_id") @db.Char(36)
  createdAt                DateTime                      @default(now()) @map("created_at") @db.DateTime(3)
  scholarshipApplication   StudentScholarshipApplication @relation(fields: [scholarshipApplicationId], references: [id], onDelete: Cascade)
  changedBy                User?                         @relation("ScholarshipStatusChangedBy", fields: [changedByUserId], references: [id], onDelete: SetNull)

  @@index([scholarshipApplicationId, createdAt])
  @@map("student_scholarship_timeline")
}

model StudentScholarshipDocument {
  scholarshipApplicationId String                        @map("scholarship_application_id") @db.Char(36)
  studentDocumentId        String                        @map("student_document_id") @db.Char(36)
  scholarshipApplication   StudentScholarshipApplication @relation(fields: [scholarshipApplicationId], references: [id], onDelete: Cascade)
  studentDocument          StudentDocument               @relation(fields: [studentDocumentId], references: [id], onDelete: Cascade)

  @@id([scholarshipApplicationId, studentDocumentId])
  @@index([studentDocumentId])
  @@map("student_scholarship_documents")
}

model StudentConsultantAssignment {
  id               String                @id @default(uuid()) @db.Char(36)
  studentProfileId String                @map("student_profile_id") @db.Char(36)
  consultantId     String                @map("consultant_id") @db.Char(36)
  status           String                @default("ACTIVE") @db.VarChar(20)
  assignedAt       DateTime              @default(now()) @map("assigned_at") @db.DateTime(3)
  endedAt          DateTime?             @map("ended_at") @db.DateTime(3)
  studentProfile   StudentProfile        @relation(fields: [studentProfileId], references: [id], onDelete: Cascade)
  consultant       Consultant            @relation(fields: [consultantId], references: [id], onDelete: Restrict)
  conversations    StudentConversation[]

  @@index([studentProfileId, status])
  @@index([consultantId, status])
  @@map("student_consultant_assignments")
}

model StudentConversation {
  id                     String                       @id @default(uuid()) @db.Char(36)
  studentProfileId       String                       @map("student_profile_id") @db.Char(36)
  consultantAssignmentId String?                      @map("consultant_assignment_id") @db.Char(36)
  createdAt              DateTime                     @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt              DateTime                     @updatedAt @map("updated_at") @db.DateTime(3)
  studentProfile         StudentProfile               @relation(fields: [studentProfileId], references: [id], onDelete: Cascade)
  consultantAssignment   StudentConsultantAssignment? @relation(fields: [consultantAssignmentId], references: [id], onDelete: SetNull)
  messages               StudentMessage[]

  @@index([studentProfileId, updatedAt])
  @@map("student_conversations")
}

model StudentMessage {
  id               String              @id @default(uuid()) @db.Char(36)
  conversationId   String              @map("conversation_id") @db.Char(36)
  senderType       String              @map("sender_type") @db.VarChar(20)
  studentSenderId  String?             @map("student_sender_id") @db.Char(36)
  internalSenderId String?             @map("internal_sender_id") @db.Char(36)
  body             String              @db.Text
  readAt           DateTime?           @map("read_at") @db.DateTime(3)
  createdAt        DateTime            @default(now()) @map("created_at") @db.DateTime(3)
  conversation     StudentConversation @relation(fields: [conversationId], references: [id], onDelete: Cascade)
  studentSender    User?               @relation("StudentMessageSender", fields: [studentSenderId], references: [id], onDelete: SetNull)
  internalSender   User?               @relation("InternalMessageSender", fields: [internalSenderId], references: [id], onDelete: SetNull)

  @@index([conversationId, createdAt])
  @@map("student_messages")
}

model StudentNotification {
  id               String         @id @default(uuid()) @db.Char(36)
  studentProfileId String         @map("student_profile_id") @db.Char(36)
  type             String         @db.VarChar(50)
  title            String         @db.VarChar(255)
  body             String?        @db.VarChar(1000)
  href             String?        @db.VarChar(2048)
  readAt           DateTime?      @map("read_at") @db.DateTime(3)
  createdAt        DateTime       @default(now()) @map("created_at") @db.DateTime(3)
  studentProfile   StudentProfile @relation(fields: [studentProfileId], references: [id], onDelete: Cascade)

  @@index([studentProfileId, readAt, createdAt])
  @@map("student_notifications")
}

model StudentSupportTicket {
  id                  String                  @id @default(uuid()) @db.Char(36)
  studentProfileId    String                  @map("student_profile_id") @db.Char(36)
  category            String                  @db.VarChar(30)
  subject             String                  @db.VarChar(255)
  status              String                  @default("OPEN") @db.VarChar(30)
  lastChangedByUserId String?                 @map("last_changed_by_user_id") @db.Char(36)
  createdAt           DateTime                @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt           DateTime                @updatedAt @map("updated_at") @db.DateTime(3)
  studentProfile      StudentProfile          @relation(fields: [studentProfileId], references: [id], onDelete: Cascade)
  lastChangedBy       User?                   @relation("SupportStatusChangedBy", fields: [lastChangedByUserId], references: [id], onDelete: SetNull)
  messages            StudentSupportMessage[]

  @@index([studentProfileId, status, updatedAt])
  @@map("student_support_tickets")
}

model StudentSupportMessage {
  id           String               @id @default(uuid()) @db.Char(36)
  ticketId     String               @map("ticket_id") @db.Char(36)
  senderType   String               @map("sender_type") @db.VarChar(20)
  senderUserId String?              @map("sender_user_id") @db.Char(36)
  body         String               @db.Text
  createdAt    DateTime             @default(now()) @map("created_at") @db.DateTime(3)
  ticket       StudentSupportTicket @relation(fields: [ticketId], references: [id], onDelete: Cascade)
  sender       User?                @relation("SupportMessageSender", fields: [senderUserId], references: [id], onDelete: SetNull)

  @@index([ticketId, createdAt])
  @@map("student_support_messages")
}

model StudentReferral {
  id                String         @id @default(uuid()) @db.Char(36)
  referrerProfileId String         @map("referrer_profile_id") @db.Char(36)
  referredProfileId String         @unique @map("referred_profile_id") @db.Char(36)
  referralCode      String         @map("referral_code") @db.VarChar(40)
  stage             String         @default("REFERRED") @db.VarChar(40)
  rewardStatus      String         @default("NOT_ELIGIBLE") @map("reward_status") @db.VarChar(30)
  rewardAmount      Decimal?       @map("reward_amount") @db.Decimal(12, 2)
  rewardCurrency    String?        @map("reward_currency") @db.Char(3)
  createdAt         DateTime       @default(now()) @map("created_at") @db.DateTime(3)
  updatedAt         DateTime       @updatedAt @map("updated_at") @db.DateTime(3)
  referrerProfile   StudentProfile @relation("StudentReferralReferrer", fields: [referrerProfileId], references: [id], onDelete: Cascade)
  referredProfile   StudentProfile @relation("StudentReferralReferred", fields: [referredProfileId], references: [id], onDelete: Cascade)

  @@unique([referrerProfileId, referredProfileId])
  @@index([referrerProfileId, stage])
  @@map("student_referrals")
}
